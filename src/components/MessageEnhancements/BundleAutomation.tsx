// Phase 9: Personalization & Automation Bundle
// Pruned: only the formatting toolbar remains. Auto-response rules,
// contact notes, conversation modes, notification sounds, and draft
// manager were removed (stubs / out-of-scope for the message tools
// surface). The remaining stubs that belong in global Settings (sounds,
// modes, notifications) are not message tools.

import { FormattingToolbar, InlineFormattingBar } from './FormattingToolbar';

export default {
  FormattingToolbar,
  InlineFormattingBar,
};
