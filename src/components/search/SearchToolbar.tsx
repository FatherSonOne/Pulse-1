// SearchToolbar — top strip: search input + view toggle [Table|Cards|Map] +
// filter/clipboard toggles + save + export. Phase 2.
//
// Spec: docs/SEARCH_WORKBENCH_REDESIGN_HANDOFF_2026-05-30.md §4.1
export default function SearchToolbar() {
  return null;
}
