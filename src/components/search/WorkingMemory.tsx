// WorkingMemory — empty-state blocks shown in the center when the query is
// empty: Resume (kbd 1–5), Recent threads, Pinned, Browse-by-type, first-run
// onboarding. Phase 7.
//
// Spec: docs/SEARCH_WORKBENCH_REDESIGN_HANDOFF_2026-05-30.md §4.7
export default function WorkingMemory() {
  return null;
}
