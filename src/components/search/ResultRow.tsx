// ResultRow — one dense table row (Type · Subject/preview · Person · When).
// MUST keep role="article", tabIndex={0}, data-result-id so the ported
// results-keydown handler (↑↓→← Enter Esc) works unchanged. Inline peek =
// expand-in-place. Phase 4.
//
// Spec: docs/SEARCH_WORKBENCH_REDESIGN_HANDOFF_2026-05-30.md §4.3, Invariant #5
export default function ResultRow() {
  return null;
}
