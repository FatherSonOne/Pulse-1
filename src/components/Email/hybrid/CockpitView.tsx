// CockpitView — Phase 1 will mount BriefingHeader + SignalSection + LaneSection + rail.
import React from 'react';

export const CockpitView: React.FC = () => {
  return <div data-stub="CockpitView" />;
};

export default CockpitView;
