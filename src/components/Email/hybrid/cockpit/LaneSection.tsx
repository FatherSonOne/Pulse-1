// LaneSection — Phase 1 stub. Collapsible category lane (Work / Admin / News / Personal).
import React from 'react';

export const LaneSection: React.FC = () => {
  return <div data-stub="LaneSection" />;
};

export default LaneSection;
