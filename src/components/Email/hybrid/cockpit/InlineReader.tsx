// InlineReader — Phase 1/2 stub. Expanded body + thread + actions inside a SignalRow.
import React from 'react';

export const InlineReader: React.FC = () => {
  return <div data-stub="InlineReader" />;
};

export default InlineReader;
