// TriageDone — Phase 3 stub. End state with halo + per-session stats.
import React from 'react';

export const TriageDone: React.FC = () => {
  return <div data-stub="TriageDone" />;
};

export default TriageDone;
