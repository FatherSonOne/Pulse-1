// SegmentedModeToggle — Phase 3 stub. [⌘ Cockpit | ◇ Triage · N] pill + ⌘E hint.
import React from 'react';

export const SegmentedModeToggle: React.FC = () => {
  return <div data-stub="SegmentedModeToggle" />;
};

export default SegmentedModeToggle;
