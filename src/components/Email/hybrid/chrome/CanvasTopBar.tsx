// CanvasTopBar — Phase 7 stub. Assembles SegmentedModeToggle + FoldersDropdown + Search + Sync + Settings.
import React from 'react';

export const CanvasTopBar: React.FC = () => {
  return <div data-stub="CanvasTopBar" />;
};

export default CanvasTopBar;
