// FoldersDropdown — Phase 6 stub. Popover for Starred / Snoozed / Sent / Drafts / etc.
import React from 'react';

export const FoldersDropdown: React.FC = () => {
  return <div data-stub="FoldersDropdown" />;
};

export default FoldersDropdown;
