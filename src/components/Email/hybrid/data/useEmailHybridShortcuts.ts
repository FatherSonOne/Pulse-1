// useEmailHybridShortcuts — Phase 5 stub. Wraps useEmailKeyboardShortcuts and
// layers on ⌘E / Ctrl+E (mode toggle), T (push to task in Triage), H (snooze
// default in Triage), plus mode-aware Esc and j/k semantics.
export function useEmailHybridShortcuts() {
  // no-op until Phase 5
}
