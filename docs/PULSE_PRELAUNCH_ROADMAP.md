# Pulse Pre-Launch Roadmap (Living Document)

**Created:** 2026-05-25 · **Owner:** solo (Aegis{FM}) · **Tracking epic:** [#98](https://github.com/FatherSonOne/Pulse-1/issues/98)
**Worked by:** the `/launch-prep` slash command — see [How the agent uses this doc](#how-the-agent-uses-this-doc).

> **Organizing principle (decided 2026-05-25): ship only what is real; feature-flag or hide the rest for v1.**
> The biggest launch risk is not any single gap — it is *truth-in-product*: showing a UI that advertises features (SMS, push, enrichment, meeting AI) that don't actually work. That erodes trust on day one. Every roadmap item serves either "make it real" or "stop showing what isn't."

This document is the single source of truth for launch readiness. It is **living**: the `/launch-prep` agent updates the [Status Table](#status-table) and [Resume Pointer](#resume-pointer) after every issue it completes, so the next run picks up where the last left off.

---

## How the agent uses this doc

`/launch-prep` runs **one issue per invocation**:

1. Read this doc (especially the [Resume Pointer](#resume-pointer) and [Status Table](#status-table)).
2. `gh issue list --label launch-roadmap --state open` → pick the highest-priority **unblocked** issue (P0 → P1 → P2; respect the **Depends-on** column).
3. Implement it on `main` per `CLAUDE.md` git discipline (commit each unit, conventional commits, `Co-Authored-By` trailer; **never branch without asking**; **pause-and-verify** any uncommitted work it didn't author).
4. On completion: comment on the issue with what shipped + evidence, close it (or mark `status: blocked` with the blocker), then update the [Status Table](#status-table) row and the [Resume Pointer](#resume-pointer) in this doc and commit that.
5. Report a short summary and stop. (One issue per run keeps history readable and lets the human review between steps.)

If the chosen issue turns out to be a product decision rather than a code task (e.g. CALEA legal read, E2EE positioning), the agent drafts the decision doc + options and asks the human rather than guessing.

---

## Audit summary (the 11 questions)

1. **Market.** Sits across Unified Communications & Collaboration (~$113–186B 2025, ~28% CAGR to ~$634B by 2030), team-messaging, and "work hub." The Western "everything app" thesis is unproven — treat all-in-one as a *convenience wedge*. Honest home: **AI-native unified comms hub for SMB teams + customer-facing roles.**
2. **Competitors.** Slack/Teams/Discord (chat), Voxer (PTT), Front/Missive/Spike (shared inbox), Beeper/Texts (unified messaging), Notion/ClickUp (work hub), Superhuman (AI email). **AI is table stakes** — everyone ships summaries + smart compose.
3. **Retention.** Headline = **DAU/MAU stickiness** (messaging 50–70%, collab 25–45%, Slack ~40–60%). Plus D1/D7/D30 curves, churn/NRR. **Key finding:** stickiness is a *multiplayer* property — Slack teams at ~2,000 messages hit 93% weekly retention; solo users barely retain.
4. **Launch-ready when:** every advertised surface works E2E; integration layer runs in prod (not localhost); SMS is 10DLC + TCPA compliant; email + push actually deliver; GDPR/CCPA + ToS live; retention instrumentation wired; team-activation onboarding exists.
5. **Under the hood.** **Productivity core is genuinely real:** Decisions+voting, Tasks/Subtasks, Contacts + vCard cards, Calendar (Google/Outlook + RRULE + booking), **Billing** (production-grade, 9 edge fns), Auth/MFA/biometric, **Relay voice** (4 modes + transcription), **Maps** (~85% real), Search, disciplined **AI-router**. **Comms-aggregation pitch is where demo ≠ reality** — see [Capability Matrix](#capability-matrix).
6. **First-use wow (real today):** one inbox + AI thread summaries, Relay async voice, vCard contact cards, Decisions voting, Maps route/week AI, cross-surface `Ctrl+/` assistant. Risk = hitting mocked SMS / never-arriving push right after.
7. **Gap vs competitors.** Jack-of-all-trades depth deficit; multiplayer cold-start ×N surfaces; switching costs; notification fatigue (funneling more channels in can amplify it).
8. **Close the gap.** Lead with cross-surface AI; make consolidation genuinely reduce switching+notifications; activate teams not individuals; ship only what's real; pick a v1 lane.
9. **Differentiators.** Cross-surface AI over one data model; Relay voice modes bundled with productivity; vCard contact cards; Decisions-with-voting; maps intelligence inside comms; disciplined metered AI-router + real billing.
10. **Risks.** SMS (10DLC blocking + TCPA $500–1,500/text + one-to-one consent Jan 27 2026); GDPR/CCPA + residency; CALEA (voice/video VoIP?); E2EE-vs-server-AI trust tension; deliverability; vendor lock-in (Twilio/Daily/Supabase); ~99.9% uptime bar; moderation/abuse.
11. **Else.** Follows the repo's existing issue convention (epic + `priority:*` + `type:*` + section labels). Truth-in-product is the through-line.

Full market evidence (with source URLs) and code evidence (file:line) are captured in the individual GitHub issues.

---

## Capability Matrix (verified 2026-05-25)

✅ REAL (works E2E) · 🟡 PARTIAL · 🔴 STUB / mocked / unwired

| Surface | Verdict | Note |
|---|---|---|
| Decisions + voting, Tasks, Subtasks | ✅ | Decisions/Tasks/Subtasks DB-backed + AI decision wizard. **Voting reconciled ✅ (#125, 2026-05-30):** end-user decision voting is **real + DB-backed** — the Decisions Cockpit (`DecisionDetail.handleVote` + CockpitHub quick-approve) calls `decisionService.castVote()` → live `public.decision_votes` (RLS on, `UNIQUE(decision_id,user_id)`, persists approve/reject/concern/abstain; verified in prod: table exists, real vote rows). Ships **ungated** to all users. The `proposalMode` flag (OFF/internal) gates a *separate* surface — the in-Messages composer proposal mode whose votes are simulated client-side — not this. Misleading flag description fixed |
| Contacts + vCard contact-card sharing | ✅ | Differentiated; full edge-fn suite |
| Calendar (Google/Outlook, RRULE, booking) | ✅ | Token refresh via the deployed Render backend (#99) |
| Billing (Stripe) | ✅ | Production-grade; 9 edge functions |
| Auth / MFA / biometric | ✅ | Supabase OAuth + TOTP + WebAuthn |
| Relay voice (Direct/Channel/Broadcast/Notes) | ✅ | Real storage + tables + multi-provider transcription |
| Maps / location | ✅ (~85%) | geocode/directions/distance + real geofencing + AI route proposals |
| Unified Search | ✅ | Multi-source Supabase queries |
| AI features (summaries, compose, autopilot, RAG) | ✅ | All via metered `ai-router`, no client keys |
| Video meetings (Daily.co) | ✅ | `daily-rooms` edge fn |
| Unified Inbox | 🟡 | Aggregation real; Slack feeder now routes to the deployed backend (#99, route+CORS verified, live smoke pending); SMS hidden for v1 (#100); email/Pulse not auto-synced in |
| Email (Gmail) | 🟡 | Send real; token refresh via the deployed backend (#99, route verified, live refresh smoke pending) |
| CRM sync | 🟡 | **OAuth connect now works server-side** (#99, `5ed5f94`): new Node-safe `server/crmOAuth.js` (browser-free `exchangeCodeForToken`) — the `/api/crm/callback/:platform` route no longer throws. Frontend client-id bug fixed (`f6d9119`). **Still partial:** the manual-sync route (`server.js:911`) still imports the browser-only `crmService.ts` (throws under Node), and there's no pagination → both handed to **#108**. Live provider round-trip = human smoke (#99) |
| Post-meeting AI | ✅ | **Native is REAL** (#106): Deepgram live transcription + ai-router `generateSummary` → action-items/decisions/keyPoints on Pulse-hosted meetings, persisted to `pulse_video_rooms`. **Meeting Analytics now native too** (#121, `a1be70e`): `fetchMeetingAnalytics` repointed off the orphaned `meetings` table onto `pulse_video_rooms` (`status='ended'`) — count/hours/avg/8-week-trend from `duration_seconds`, and sentiment/topics/decisions parsed from the server-generated Gemini `summary` JSON. Entomate hard-gate removed (the native claim is now TRUE); honest empty state when no ended meetings; `topAttendees` permanently empty (no native attendees column) |
| Native Android / Electron | 🟡 | Electron builds exist; **Android release signing wired + signed AAB verified** (#103, `797a132`) — `npm run android:build` → signed 20.78 MB AAB; keystore valid to 2053, gitignored. Remaining: enroll Play App Signing + upload (human) |
| **In-app SMS** | 🔴 → hidden | **100% mocked** (`smsService.isMockMode → true`); **gated OFF for v1** behind `inAppSms` flag (#100, `f1b9e49`). Real wiring tracked in #120 |
| **Push notifications** | ✅ | `send-push` VAPID dispatcher deployed (pulse-chat) + wired to search alerts; verified end-to-end (live `sent:1` to a real FCM subscription, #101). All enable-paths now subscribe; handler injected into the VitePWA SW |
| Email campaigns | 🔴 (unsafe) | Naive per-recipient loop → spam-flag risk |
| "Contact enrichment" | ✅ (relabeled) | #106-style honesty pass (#107, `c0d0d12`): UI was already honest (dedup/merge, no "enrichment" CTA/marketing). Internal mislabel relabeled comment-only — signature-parse + internal `relationship_profiles` dedup/merge, **no external source**. Cross-app `server.js` `/enrich` route + ai-router `contact_enrichment` task name left intact (contract/router risk) |
| Data-subject rights (export / erasure / residency) | ✅ | #111 (`0ac2922`): self-serve export (`DataExportRequestCard`, live ~21-table read, GDPR Art. 20) + **complete erasure** (`delete-account` edge fn → expanded `delete_user_account` RPC incl. canonical `pulse_users` + NO-ACTION-FK clears, then `auth.admin.deleteUser`). Residency = us-east-1 (US) + SCCs, documented in `docs/DSAR_RUNBOOK.md` (ROPA, AI-processing note, intake + SLA). Human: set `VITE_SUPABASE_REGION` in Vercel; export still omits contacts/calendar/email categories (documented gap) |
| Public legal docs (ToS, Privacy, AUP, abuse/DMCA) | ✅ | #112 (in `a4d5006` co-commit): ToS §13 names **South Carolina** + venue; canonical contact **`fm1@qntmecos.com`** everywhere. Privacy §5 — **false "anonymized" AI claim removed**, provider set reconciled to Gemini+Claude+OpenAI (dropped unevidenced AssemblyAI/ElevenLabs). §2.4 honest SMS-v1-disabled note. §10 points to real Settings→Privacy tools. New `docs/ABUSE_TAKEDOWN_RUNBOOK.md`: Acceptable Use, CSAM/NCMEC, **17 U.S.C. §512(c)(3) DMCA** + counter-notice + repeat-infringer, real `admin-manage-user` enforcement, email reporting (no in-app Report button in v1) |

**The latent blocker behind several rows is mostly resolved (2026-05-26):** the second backend (`server.js`, Express) is now **deployed on Render** at `https://pulse-api-1epw.onrender.com` (`npm run server`, `$PORT`-aware, health check at `/api/health`), and the frontend is rebuilt with `VITE_BACKEND_URL` pointing at it (verified inlined in the deployed bundles). The Slack/Twilio/Gmail/health routes are **mounted, input-validating, and CORS-correct** for `pulse.logosvision.org`; **live-account round-trips PASSED 2026-05-31** — real Slack channel pull (bot-token, sender resolved) + real Gmail fetch (10 emails) both verified from production. **Architecture clarification surfaced during the smoke:** these are **proxy/credential flows, NOT OAuth redirects** — Slack = bring-your-own bot token, Gmail = Google-sign-in piggyback (no `/api/slack/callback` or `/api/gmail/callback` route ever existed; see `docs/INTEGRATIONS_BUILT_VS_STUB_AUDIT_2026-05-31.md`). **Two pieces remain deferred:** (1) **CRM OAuth** can't run server-side — `oauthHelper`/`crmService` import the browser-only `supabase` client (`import.meta.env`), so the `/api/crm/callback` path throws until refactored onto the service-role client; (2) the **Logos Vision contacts-import** Google flow hardcodes a `localhost:5176` redirect and is LV-secret-gated. → [#99](https://github.com/FatherSonOne/Pulse-1/issues/99).

---

## Status Table

Statuses: `open` · `in-progress` · `blocked` · `done`. The agent edits the **Status** and **Notes** cells as it works.

### P0 — Launch blockers (working core)

| # | Title | Priority | Depends on | Status | Notes |
|---|---|---|---|---|---|
| [#99](https://github.com/FatherSonOne/Pulse-1/issues/99) | Deploy server.js backend + remove hardcoded localhost:3003 | critical | — | open (code done; **human smoke**) | **Deployed** on Render (`pulse-api-1epw.onrender.com`) via Blueprint (`90b922d`/`c293bfe`/`f6d9119`); `VITE_BACKEND_URL` set in Vercel + inlined; Slack/Twilio/Gmail/health routes + CORS verified. **CRM OAuth callback now functional server-side** (`5ed5f94`): root cause was a `.ts`-import-under-plain-node bug + browser-supabase coupling; fixed via Node-safe `server/crmOAuth.js`. Verified `node` loads it + `node --check server.js`. **Slack + Gmail live-smoke PASSED 2026-05-31** (bot-token + Google-piggyback, not OAuth — see INTEGRATIONS_BUILT_VS_STUB audit). **Remaining (all human / handed-off):** (1) CRM live round-trip — register CRM redirect URIs + creds on Render, connect a CRM; (2) the `crmService.fullSync` sync-engine Node port → **#108**; (3) LV-bridge `localhost:5176` redirect. Non-blocking bugs found in smoke: "Connected to undefined" (slackService field-name mismatch) + synced messages have no prod reader (UnifiedInbox unrouted) |
| [#100](https://github.com/FatherSonOne/Pulse-1/issues/100) | Flag/hide the mocked in-app SMS surface for v1 | critical | — | done | `f1b9e49` — `inAppSms` flag OFF; nav hidden + route redirects to Dashboard. Follow-up #120 owns real wiring |
| [#101](https://github.com/FatherSonOne/Pulse-1/issues/101) | Build push dispatch path (or flag push off) | critical | — | done | `send-push` deployed + VAPID/secret set; live `sent:1` to a real FCM subscription. Reconciled 4 disconnected enable-paths → subscribe; push handler injected into VitePWA SW; always-visible Enable/Disable card |
| [#102](https://github.com/FatherSonOne/Pulse-1/issues/102) | Transactional email deliverability — verified domain | critical | — | done | **CLOSED 2026-05-26.** `ec73a65` aligned all senders to `pulse.logosvision.org`; 3 edge fns deployed to `pulse-chat` (send-email v15 / send-security-alert v34 / check-search-alerts v26, `verify_jwt=false` preserved via `5b190cc`). Delivery test PASSED (200, id `f314ac89…` → `fm1@qntmecos.com`); SPF+DKIM pass. Client `VITE_RESEND_API_KEY` repointed to the `fm1` send-only key (Vercel + `.env`). **DMARC TXT record now SET by operator** — final acceptance item done. Residual browser-key-exposure hardening (route invite/RSVP through `send-email` edge fn) tracked as `TODO(#104)`. Runbook: `docs/EMAIL_DELIVERABILITY_RUNBOOK.md` |
| [#103](https://github.com/FatherSonOne/Pulse-1/issues/103) | Android release signing (keystore) | high | — | done | `797a132` — issue evidence stale (keystore + config already existed). Hardened `build.gradle` (keystore.properties → `PULSE_KEYSTORE_*` env → defaults, `canSignRelease` guard); `npm run android:build` now produces a **verified signed AAB** (20.78 MB, `pulse-key` sig block); runbook `docs/ANDROID_RELEASE_RUNBOOK.md`. Human advisory only: offline-backup keystore + enroll Play App Signing at upload |

### P1 — Truth-in-product

| # | Title | Priority | Depends on | Status | Notes |
|---|---|---|---|---|---|
| [#104](https://github.com/FatherSonOne/Pulse-1/issues/104) | Feature-flag audit — gate every non-real surface | high | — | open (audit DONE) | `72c7571` — inventory of all ~18 `AppView` surfaces in `docs/FEATURE_FLAG_AUDIT_v1.md`. **Only hard mock in `src/` is `smsService` (already gated via `inAppSms` #100); no orphan stub needed gating.** Remaining non-real surfaces are owned by #105/#106/#107/#108 (deferred, no scope creep). **Kept open as the truth-in-product umbrella** — close when #105/#106/#107 land + #115 click-through passes. **#105 ✅ + #106 ✅ + #107 ✅ all done — the only remaining gate is the #115 human QA click-through.** Eligible to close once that passes. Do NOT re-pick for code |
| [#105](https://github.com/FatherSonOne/Pulse-1/issues/105) | Email campaigns — flag for v1 (or harden) | high | — | done | `0767141` — operator chose **hide** (feature likely cut). New `emailCampaigns` flag OFF; email sidebar "Tools" section hidden + render guard + stale-state reset → unsafe per-recipient send loop unreachable. Compliant batched send is the deferred unhide condition. Dev preview `?ff_emailCampaigns=on` |
| [#106](https://github.com/FatherSonOne/Pulse-1/issues/106) | Post-meeting AI — own it or relabel Entomate handoff | high | — | done | `06f3554` — native transcription+summary already REAL (acceptance #1, untouched). Acceptance #2: the orphaned Meeting Analytics surface (reads `meetings` table, **no writer** anywhere — native→`pulse_video_rooms`, Entomate→bot recap msg) gated on `isEntomateConnected()` + honest empty states; false "AI Scribe populates analytics" copy removed; `MeetingsRailKicker` dashboard stats hidden when not connected. Write-back gap → follow-up **#121** |
| [#107](https://github.com/FatherSonOne/Pulse-1/issues/107) | Relabel 'contact enrichment' (it's dedup) | medium | — | done | `c0d0d12` — **UI was already honest** (ContactsRedesigned/DuplicateDetectionModal say "duplicates"/"merge"; `enrichFromSignature` has no UI surface; no marketing copy). Relabeled internal mislabel comment-only: service header `CONTACT ENRICHMENT` → `CONTACT PROFILE` + "NOT third-party" note; `server.js` `/enrich` endpoint comment. **Left intact** (regression risk): cross-app `/enrich` route + `enrichment` response key, ai-router `contact_enrichment` task, service/file names |
| [#108](https://github.com/FatherSonOne/Pulse-1/issues/108) | CRM sync pagination (limit:100 one-shot) | medium | #99 (OAuth ✅) | open | **Scope grew (#99 handoff):** beyond pagination, now also owns porting `crmService.fullSync` (+ provider services) off the browser supabase client so `server.js:911`'s manual-sync route runs under Node (decouple à la the #99 callback fix, or move sync to an edge fn). OAuth connect (the #99 dependency) is now functional. Both need live provider creds to verify |
| [#124](https://github.com/FatherSonOne/Pulse-1/issues/124) | Public copy (README + landing FAQ) advertises non-real features | high | ~~#113~~ ✅ | done | **CLOSED 2026-05-30.** All 3 public surfaces truthful. README applied `ece9a15`; landing FAQ/pricing `20283c5` (AI-vendor → Gemini+Claude+OpenAI, E2EE/"support can't read" overclaim → honest #113 Option-B wording, SMS dropped from surface list + both pricing tiers); README hedge → guide `2bc40a5`. Verified: no public claim contradicts Privacy Policy / `package.json` / an OFF flag. Follow-up (not public copy): quarantine orphaned `encryption.ts` under #104 | **From 2026-05-29 review (R-01).** Public-copy sibling to #104 (which gated in-app surfaces). README claims E2EE (`README.md:132,423`) + React 19 (ships 18.2) + v27 (is 25.1.3) + Voxer (now Relay); FAQ lists ElevenLabs/AssemblyAI/Whisper (dropped from Privacy §5 in #112). Honest README **APPLIED** 2026-05-29 (`README.md` rewritten — E2EE/React19/v27/Voxer/stale-push/abandoned-contact all fixed; TS badge corrected 5.6→5.8 at apply time). **Remaining under #124:** landing FAQ vendor reconcile (`landingData.ts:10,43` drop ElevenLabs/AssemblyAI/Whisper), FAQ E2EE/"support can't read" line, SMS in pricing/headline copy. Done = no public claim contradicted by Privacy Policy / `package.json` / an OFF flag |
| [#125](https://github.com/FatherSonOne/Pulse-1/issues/125) | Reconcile Decisions "voting" (proposalMode OFF/internal vs marketed-as-shipped) | medium | — | done | **Verdict: claim is TRUE; flag naming was the problem.** Real end-user voting ships ungated via the Decisions Cockpit → `decisionService.castVote()` → live `decision_votes` (verified in prod DB: table + UNIQUE constraint + RLS + real rows). `proposalMode` (OFF) gates only a separate in-Messages simulated-vote surface. Fix: disambiguated the misleading `proposalMode` comment + `description` in `featureFlags.ts`; restored Matrix row to ✅; relaxed `PULSE_MARKETING_COPY_GUIDE.md` voting-in-headline note (now confirmed real). Landing "Voting" demo lane is truthful |

### P1 — Compliance / legal

| # | Title | Priority | Depends on | Status | Notes |
|---|---|---|---|---|---|
| [#109](https://github.com/FatherSonOne/Pulse-1/issues/109) | SMS — A2P 10DLC + TCPA one-to-one consent | high | — | open (**deferred to v1.1**) | Gates real SMS. **2026-05-30 strategy decision:** SMS is a **v1.1** item (operator) — review pressure (R-03) already resolved by #100+#124; 10DLC clock **not** started now. Becomes a `/launch-followups` human task only if SMS is committed for v1.1. Rationale: `docs/PULSE_SMS_STRATEGY_GUIDE.md` |
| [#110](https://github.com/FatherSonOne/Pulse-1/issues/110) | CALEA determination for voice/video | medium | — | done | `2515b4f`+`9c89b56` — `docs/PULSE_CALEA_DETERMINATION_GUIDE.md`. **DECIDED 2026-05-31: CALEA N/A for v1, shipping on the engineering determination (operator declined a separate lawyer sign-off).** No PSTN interconnection anywhere (async app-to-app Relay/Glimpse + WebRTC Daily; verified no pstn/sip/dial-in refs) → fails FCC interconnected-VoIP prong 4 → no SSI/intercept build. **Binding tripwire:** re-evaluate (TTP/Buy) if PSTN calling is ever added (same line as #109). Closed |
| [#111](https://github.com/FatherSonOne/Pulse-1/issues/111) | GDPR/CCPA data-rights + residency | high | — | done | `0ac2922` — **erasure now complete + truthful.** New self-serve `delete-account` edge fn (deployed v1, `verify_jwt=true`, 401-verified) runs the data RPC as the user then `auth.admin.deleteUser` (prior path left `auth.users` behind → Art. 17 claim was false). Migration `dsar_erasure_completeness` (applied) expands `delete_user_account` to the no-FK tables (incl. canonical `pulse_users`) + clears all 13 NO-ACTION FK blockers. Removed fabricated mock "Download History". `docs/DSAR_RUNBOOK.md`: residency (us-east-1 US + SCCs), ROPA, server-side-AI-touches-content note, subprocessors, intake + SLA. **Human:** `VITE_SUPABASE_REGION=US East (us-east-1)` set in Vercel Prod+Preview, redeployed, bundle-verified (2026-05-27); live deletion test still needs a throwaway account |
| [#112](https://github.com/FatherSonOne/Pulse-1/issues/112) | ToS / Privacy / moderation + abuse review | high | — | done | Shipped inside co-commit **`a4d5006`** (labeled Glimpse — a parallel session ran `git commit -a` and swept my explicit-path-staged #112 files in; CLAUDE.md forbids force-push to main, so the polluted commit stands with full disclosure on the issue). Decisions: governing law **South Carolina** + venue; canonical contact **`fm1@qntmecos.com`** everywhere. ToS §13 + §6 + new "Reporting Abuse & Takedowns" §4 subsection. Privacy §5 — **removed false "anonymized or pseudonymized" AI claim**, reconciled provider set to Gemini+Claude+OpenAI (dropped unevidenced AssemblyAI/ElevenLabs). §2.4 honest SMS-v1 note. §10 points to real Settings→Privacy tools. DSAR runbook + DPA card aligned to `fm1@qntmecos.com`; runbook §4/§5 added Anthropic Claude. New `docs/ABUSE_TAKEDOWN_RUNBOOK.md` (NCMEC/CSAM, DMCA §512(c)(3), real `admin-manage-user` enforcement, email-based reporting — honest no-in-app-Report-button) |
| [#113](https://github.com/FatherSonOne/Pulse-1/issues/113) | E2EE positioning decision | medium | — | done | **DECIDED 2026-05-30 — Option B** (`dd9c5f4`, `docs/PULSE_E2EE_POSITIONING_GUIDE.md`). Honest posture: encrypted in transit (TLS) + at rest (AES-256) + RLS; server-side AI reads plaintext for opted-in features; **no E2EE**. Verified ground truth: `src/services/encryption.ts` is orphaned (0 callers) + buggy → no functioning E2EE. Canonical wording locked. **Unblocks #124.** Residual fix for #124: landing FAQ `landingData.ts:48` "support cannot read" overclaim. Follow-up: quarantine the orphaned `encryption.ts` (#104/#124) |

### P2 — Quality / reliability

| # | Title | Priority | Depends on | Status | Notes |
|---|---|---|---|---|---|
| [#114](https://github.com/FatherSonOne/Pulse-1/issues/114) | TS error burndown + CI no-new-errors gate | medium | — | done | `a249248` — type-safety net back on. New `scripts/tsc-baseline.mjs` (self-contained ESM, no new deps): full cache-free `tsc` with the 8GB heap injected into the child (cross-platform, no cured OOM-false-pass), signature = `relPosixPath::TScode::normalizedMessage` (line/col excluded → no churn on unrelated edits), per-signature counts catch regressions. Frozen `tsc-baseline.json` = **1,240 errors / 583 sigs**. New `typecheck`/`typecheck:baseline`/`typecheck:gate` scripts; both `ci.yml` + `tests.yml` bare `npx tsc --noEmit` → `npm run typecheck:gate`; `.gitignore` allowlists `docs/*_BURNDOWN.md`. AC2 doc `docs/TS_ERROR_BURNDOWN.md` (top codes TS2304×338/TS2339×265/TS2593×136, top files, burndown strategy). **Baseline errors NOT touched** (additive gate). Verified New:0/exit0 on baseline; catches injected error (New:1/exit1) |
| [#115](https://github.com/FatherSonOne/Pulse-1/issues/115) | Integration test coverage (billing/video/calendar/CRM/push/SMS) | medium | — | open | |
| [#121](https://github.com/FatherSonOne/Pulse-1/issues/121) | Meeting Analytics writer (follow-up to #106) | medium | ~~#106~~ ✅ | done | `a1be70e` — **Option 1 (native).** `fetchMeetingAnalytics` repointed `meetings`→`pulse_video_rooms` (`created_by` + `status='ended'`); `duration_seconds`→min; defensive `JSON.parse` of the server-generated Gemini `summary` per row → sentiment/topics/decisions (neutral/empty fallback, never faked). Removed the now-false #106 Entomate gate on `AnalyticsModal` + `MeetingsRailKicker` (native claim is now TRUE); honest empty states preserved; People tab explicit "attendees not native" empty. No coral, no client AI, tsc no-new-errors. Gaps: attendees never native; null/plain-text summaries count toward totals but contribute neutral sentiment only |
| [#116](https://github.com/FatherSonOne/Pulse-1/issues/116) | Observability + SLOs | medium | — | open (code done; **operator infra**) | `4739d89` — **AC#3 ✅ fully + instrumentation shipped; AC#1/#2 operator-owned** (same shape as #99/#101). New `docs/PULSE_OBSERVABILITY_RUNBOOK.md` defines v1 SLOs/SLIs (28-day): message-send (≥99.5%, p95<1s), auth (valid sign-in ≥99.9%, p95<2s), AI-router (err<2%, p95<6s) + error budgets + how-measured. Wired the *defined-but-unused* `src/lib/monitoring/sentry.ts` helpers (`setUserContext`/`clearUserContext`/`captureError`/`addBreadcrumb`) into the 3 trunk seams so the SLIs are real: `AuthContext.handleLogin` (the single Google/Microsoft/Email seam — breadcrumb each attempt + `captureError surface:auth`) + `setUserContext`/`clearUserContext` co-located with #117's identify/reset; `pulseService.sendMessage` (`captureError surface:message-send` + latency breadcrumbs, real-time dedup untouched); `aiService.invokeAI` (the single ai-router chokepoint — invokeAIPrompt/invokeAIJson + all 29 geminiService exports delegate here — `captureError surface:ai-router` + latency breadcrumb). No deps/secrets/UI/coral; tsc 0-new in touched files (~1234 baseline unchanged). **Side-finding (2026-05-28, still true):** `VITE_APP_MODE=production` set in Vercel so Sentry is live in prod. **Operator remaining (runbook §6):** uptime+status-page signup (AC#1); Sentry/Supabase/vendor alert rules (AC#2); revisit SLO targets after 28d. Close when those land |

### P2 — Growth / activation / positioning

| # | Title | Priority | Depends on | Status | Notes |
|---|---|---|---|---|---|
| [#117](https://github.com/FatherSonOne/Pulse-1/issues/117) | Retention instrumentation + define NSM | high | — | done | `a3d3cf7` — `initializeMonitoring()` was dead code (never called); identify never wired; `FirstMessageSent` defined but unemit­ted. Fixed: bootstrap in `main.tsx`, identify/reset in `AuthContext` `onAuthStateChange`, trunk `trackMessageSent` in `pulseService.sendMessage`, `FirstMessageSent` + `TeammateInvited` (Resend+Gmail) emits, stale `pulsemessages.com` autocapture URL replaced. NSM = **Weekly Active Collaborative Workspaces** (≥2 members × ≥50 weekly messages). Doc: `docs/PULSE_NSM_AND_RETENTION_GUIDE.md`. **Live-prod-verified 2026-05-28** — `$identify` + `Message Sent` arrived in PostHog Live events after operator set `VITE_POSTHOG_API_KEY`/`VITE_POSTHOG_HOST`/`VITE_APP_MODE=production` in Vercel. **Dashboards built 2026-05-28** via `/launch-followups posthog-dashboards`: Stickiness Trend, Weekly + Daily Retention, Activation Funnel bundled into "Pulse — North Star" dashboard. Insight D (NSM Weekly Active Collaborative Workspaces) deferred — needs `workspace_id` on trunk `Message Sent` event, filed as #123. Recommended NSM aggregation cron (GUIDE §5.1) remains the v1 follow-up |
| [#118](https://github.com/FatherSonOne/Pulse-1/issues/118) | Team/multiplayer activation onboarding | high | — | done | `2627f6b` — v1 slice, all 3 ACs, reuse-based, no new backend. **AC1:** `OrgSetupChecklist` leads with "Invite your team" (rose "Start here" emphasis). **AC2:** new deterministic `invite-teammate` nudge in `pulseNudgesService` (fires when `canManageMembers && members<2 && workspace age <30d`; member-count proxy — full NSM ≥50-weekly-msgs half deferred, needs #123); also fixed `PulseNudgesWidget` ignoring dismissals + gated the AI chip off the deterministic nudge. **AC3:** "Invite a teammate" CTA in Relay Direct + Broadcast + Decisions "You're caught up" empty states (gated `members<2 && canManageMembers`). Shared `inviteTeammateNavigation.ts` routes to Settings→Team→invite. Rose-only, no coral. tsc no-new-errors. **Deferred enhancements:** full collaborative-depth meter (blocked on #123), post-Stripe onboarding redesign surface (5 dead `OnboardingEvent`s), more empty states |
| [#119](https://github.com/FatherSonOne/Pulse-1/issues/119) | Sharpen positioning — cross-surface AI; pick v1 lane | medium | — | done | `80c4e6b`+`9c89b56`+`d90d35a` — `docs/PULSE_POSITIONING_GUIDE.md` + `docs/PULSE_PRICING_UNIT_ECONOMICS_GUIDE.md`. **DECIDED+CLOSED 2026-05-31: Lane A (solo-first cross-surface AI hub, team as on-ramp)**; lead msg (cross-surface AI + on-every-line provenance) + differentiators ranked. Operator's pricing gate resolved: **Pulse Solo $20/mo (new hero), Team → per-seat $15/seat (min 2), Growth $300 unchanged** (solo COGS ~$3–6/mo, AI cap ~$8 worst-case, break-even ~5 Solo users). **Two follow-ups need their own issues:** (1) landing-copy swap (adopt `PULSE_MARKETING_COPY_GUIDE.md` lane-A copy + new prices); (2) Stripe pricing implementation (catalog + plans/entitlements migration + billing UI — money-touching, go-ahead-gated). `[EST]` vendor rates still to verify vs invoices |
| [#126](https://github.com/FatherSonOne/Pulse-1/issues/126) | Implement 2026 pricing — Solo $20 + per-seat Team (Stripe + plans + entitlements + billing UI) | **high** | #119 ✅ | open | Follow-up from the #119 pricing decision. **Money-touching** (TEST → smoke → LIVE). Stripe setup script scaffolded (`scripts/setup-pulse-pricing-2026.mjs`, `2134ffd`); per-seat plumbing already exists (`billing-sync-seats`/`billing-reconcile-seats`) — main gap is checkout's hardcoded `quantity:1` (`billing-checkout/index.ts:258`) + a `pulse_solo` plans row/caps + Solo billing UI. Full chain in the issue. **Operator runs the Stripe script with their key** |
| [#127](https://github.com/FatherSonOne/Pulse-1/issues/127) | Apply Lane-A landing copy + new pricing to the landing page | medium | #119 ✅ (seq after #126) | open | Adopt `PULSE_MARKETING_COPY_GUIDE.md` lane-A copy, retire "high-performance teams", show Solo $20 / Team $15-seat / Growth $300. Honesty rails (no E2EE/SMS-headline/unowned-vendors). Edits in `landingData.ts`. Sequence after #126 TEST setup so displayed prices match real checkout |
| [#123](https://github.com/FatherSonOne/Pulse-1/issues/123) | Add `workspace_id` to `Message Sent` event properties | medium | — | done | `42017f1` — typed optional `workspace_id`/`organization_id` on `trackMessageSent` (`analytics.ts:247-269`); trunk emit (`pulseService.ts:390`) attaches the sender's **active** workspace via `getCurrentWorkspaceId()` (synchronous `pulse_active_workspace` localStorage read — same key `WorkspaceContext` maintains; no DB round-trip; inside the analytics try/catch). **Issue premise corrected:** the call site had no `currentWorkspace.id` — `send_pulse_message` writes a workspace-less 1:1 `pulse_messages` row, so this is best-effort active-workspace, **omitted when unset, never faked**; `organization_id` unattached (org == workspace). Unblocks the PostHog GROUP BY; exact NSM attribution still wants the deferred Supabase cron join (GUIDE §5.6). PostHog Live-events smoke = human verify |

---

## Suggested sequencing

1. **#99** first — it unblocks the most surfaces (Slack/Twilio/Gmail/CRM) and is the single biggest demo-vs-reality gap.
2. Then the rest of P0 (**#100–#103**) — each independently makes the shipped surface honest or shippable.
3. P1 truth-in-product (**#104** drives the others) in parallel with P1 compliance reads (**#109–#113** — several are decisions, not code).
4. P2 quality + growth last, but **#117** (instrumentation) is worth pulling early so launch metrics exist from day one.

---

## Resume Pointer

> **The agent updates this block after every run.** It is the first thing the next run reads.

- **Operator DECISIONS (2026-05-31, same session as the drafts):** **#110 → DECIDED + CLOSED** — CALEA N/A for v1, shipping on the engineering determination (operator declined a separate lawyer sign-off); PSTN tripwire stands (`9c89b56`, doc status → DECIDED). **#119 → Lane A LOCKED (solo-first)** but the operator flagged the **$100/$300 prices as wrong for a solo product** and asked for a unit-economics re-eval before any landing copy. Built **`docs/PULSE_PRICING_UNIT_ECONOMICS_GUIDE.md`** (`9c89b56`): grounded in the real metered caps (Team 2k AI-msgs/$100, Growth 10k/$300) + the ai-router model mix — **per-solo-user COGS ~$3–6/mo typical, ~$12–15 heavy** (the 2k AI cap is only ~$8/mo worst-case at current model rates → high-margin), **break-even ~3–7 paying users at a $15–30 price** (fixed infra ~$65/mo), recommendation = **add a Pulse Solo tier ~$20–24/mo** (the Lane-A hero price) + **reframe the $100 Team toward per-seat (~$15–20/seat)** so solo→team is a smooth on-ramp, keep Growth $300. External vendor $/unit tagged `[EST]` pending real invoices. **#119 now DECIDED + CLOSED** (`d90d35a`): operator locked **Pulse Solo $20/mo** (new Lane-A hero, 1 seat) + **Team reframed to per-seat $15/seat** (min 2 seats — smooth solo→team on-ramp, no $100 cliff) + **Growth $300 unchanged**; pricing doc + positioning doc → DECIDED. **Two follow-ups need their own `launch-roadmap` issues (NOT yet filed):** (1) **landing-copy swap** — adopt `PULSE_MARKETING_COPY_GUIDE.md` lane-A copy with the new prices, retire "high-performance teams"; (2) **pricing implementation** — new Stripe Solo product + per-seat Team price, `plans`+`entitlements` migration, billing/checkout UI for Solo tier + seat quantity (money-touching → explicit go-ahead + fresh smoke test). Also still useful: real Anthropic/Google-AI/Daily/Supabase invoice numbers to replace the `[EST]` rates (sharpens margins, won't change the structure).
- **Prior issue:** [#119](https://github.com/FatherSonOne/Pulse-1/issues/119) + [#110](https://github.com/FatherSonOne/Pulse-1/issues/110) — **two decision drafts** (operator asked for both in one run). Drafted as decisions, not code, per the launch-prep rule. **#119 positioning** (`80c4e6b`, `docs/PULSE_POSITIONING_GUIDE.md`): lead message = cross-surface AI over one data model with on-every-line provenance (AC1); **recommends Lane A framed solo-first** — the cross-surface AI hub for the overloaded solo operator + the 1–2 they pull in, resolving the R-07 split between `PRODUCT.md` (unambiguously the solo operator, "attention saved") and the landing's unprovable "high-performance teams" claim; team is the on-ramp not the hero (also the lever for the solo-retention weakness from audit point 3); breadth stays proof-not-promise (no headlining mocked SMS / partial unified-inbox); B (customer-facing) / C (async-first) variants laid out (AC2); differentiators ranked 1–5 (AC3). Execution is cheap — `PULSE_MARKETING_COPY_GUIDE.md` is already lane-A-shaped. **#110 CALEA** (`2515b4f`, `docs/PULSE_CALEA_DETERMINATION_GUIDE.md`): engineering determination (NOT legal advice) — applied the FCC interconnected-VoIP test (47 C.F.R. §9.3); **finding: CALEA does NOT attach to v1** because the determinative prong 4 (PSTN origination+termination) is absent everywhere — Relay/Glimpse are async app-to-app voice/video messages, Daily is WebRTC app-to-app, no phone numbers / dial-in-out / SIP (verified: zero `pstn`/`sip`/`dial-in` refs in `src/services/relay/` + `supabase/functions/daily-rooms/`), SMS mocked+hidden (#100); so **no SSI report / intercept capability to build for v1** (AC2 moot); recommends a one-time telecom-lawyer confirmation + a tripwire to revisit if PSTN calling is ever added (same architectural line as #109 SMS); AC3 satisfied (decision recorded). Clean doc-only commits (`*_GUIDE.md` allowlisted), gitleaks clean, FF push; `_shots/` untouched. **Both issues commented with the recommendation + the explicit operator question; neither closed** (the operator owns the call). Once decided: #119 unblocks the landing-copy swap (file as its own issue) + #118 onboarding reframe + ASO; #110 just needs the operator to ratify "N/A for v1" ± lawyer sign-off.
- **Prior issue:** [#122](https://github.com/FatherSonOne/Pulse-1/issues/122) — **Glimpse Path D follow-ups → Tier-2 AI inbox digest**. Issue stays **OPEN** (2 human/visual items remain). Operator greenlit building the substantive code item: a real "Claude watched your inbox" cross-conversation synthesis, **flag-gated OFF for v1** (`glimpseInboxDigest`). Shipped `5223a18` (6 files, +371/-3). **Architecture:** extended the existing `ai-router` edge fn with a new `glimpse_inbox_digest` task (Claude Haiku primary, Gemini Flash fallback, 512 max tokens, cached system prompt) rather than a standalone fn — inherits metering/fallback/caching + keeps the single server-side chokepoint; the client only builds a prompt from `GlimpseConversation` data it already holds and calls `invokeAIJson` (the model call is server-side → satisfies the issue's "server-side only, never a direct Gemini call from React"; verified no model SDK in changed files). New `useGlimpseInboxDigest` hook: ≤12-conv needs-first prompt, defensive `{headline,highlights[]}` coercion (drops highlights whose conversationId wasn't sent), AbortController on unmount/inbox-change, stable-signature dedupe; **gates to idle (never calls the router) when flag-off / no needs / no workspace**, any throw → error → Tier-1. `GlimpseBriefingCard` now takes `aiEnabled`/`digestState`: ready → Claude headline replaces the templated line + a visible **muted "Claude" provenance chip** + clickable highlights; loading → Tier-1 + pending chip; idle/error/flag-off → **pure Tier-1** (the default prod path, zero visual change). **Truth-in-product:** the deterministic counts + Needs-you list ALWAYS render — AI prose never replaces real numbers. Files: `ai-router/tasks.ts` + client mirror `ai/types.ts` (new task in both unions), `featureFlags.ts` (flag OFF + deploy-ordering comment), `useGlimpseInboxDigest.ts` (new), `Glimpse.tsx` (props + AI render + flag/hook wiring), `Glimpse.css` (`.gl-briefing-ai-line`/`-highlights`, `--gl-*` tokens only, coral on AI surface). tsc gate New:0. **⚠️ Operator handoff before enabling:** `supabase functions deploy ai-router` (picks up the new task — un-redeployed returns `invalid_task` → client falls back to Tier-1, safe-but-inert), then flip the flag. Clean explicit-path commit on top of the parallel session's latest (clean FF push); `_shots/` + parallel auth/migration WIP untouched. **Still open under #122:** visual pass (`_design-playground/_shots/glf-01..04`, now incl. Tier-2 ready/loading states) + Glimpse e2e a11y spec — both human/visual-gated, no Claude code slice.
- **Prior issue:** [#122](https://github.com/FatherSonOne/Pulse-1/issues/122) — **Glimpse Path D follow-ups → Cleanup item only**. Issue stays **OPEN** (3 of 4 deferred items remain). Operator picked the **dead-code cleanup** slice. Shipped (`38254b6`, net **7 insertions / 382 deletions** across 2 Glimpse files): removed the retained pre-redesign Triage Cockpit row now that the reel grid is verified — `Glimpse.tsx` lost the unused `ConversationItem` component (**zero** `<ConversationItem` JSX usages repo-wide) + comment; `Glimpse.css` lost the table-only rule blocks (`.gl-tc-list/-colhead/-row`(+pseudos/`[data-unread]`)`/-avatar*/-signal/-from*/-unread-dot/-kind/-summary*/-skeleton/-actions/-duration/-when/-counter*`), the responsive `@media` "row collapse" subsection, and 2 dead reduced-motion selectors. **Surgical, not blanket** — the orchestrator pre-built a verified 3-way keep/delete inventory because `gl-tc-*` is NOT all dead: **KEPT** `.gl-tc-action-pill`(+dark) + `.gl-tc-ai-chip` (reused by the live `ReelCard`) and the `.gl-tc-empty*` block (live empty state, + its reduced-motion entries); the `.gl-tc-counter*` classes had zero references so they went with the dead set. Worker did the mechanical deletion; orchestrator verified independently (kept classes still defined+referenced, dead selectors grep=0, CSS braces 445/445 balanced, tsc gate no-new-errors). **Parallel-session note:** a second Claude session in the shared `f:/pulse1` working tree committed unrelated auth-signup WIP (`98c43e3`/`8d583e7`) + a privacy erasure fix (`d99381a`) mid-run; the user had said "leave it, it's my WIP" so I committed ONLY my own files with explicit paths — my 2 commits stacked cleanly on top, clean FF push, the auth WIP was never sweeded (it was the parallel session's own commits, not lost). **Still open under #122:** Tier-2 AI inbox-digest edge fn (the substantive remaining code item — needs a build-for-v1 decision), visual pass (human eyeball), e2e a11y spec (needs live auth fixture).
- **Prior issue:** [#114](https://github.com/FatherSonOne/Pulse-1/issues/114) — **TS error burndown + CI no-new-errors gate**. **DONE + closed** (`a249248`; **portability follow-up `948b8a3`**). The type-safety net was effectively off: Vite/esbuild skip type-checking, so ~1,240 `tsc --noEmit` errors accumulated, and the two CI steps that ran tsc (`ci.yml:34`, `tests.yml:309`) ran a bare `npx tsc --noEmit` with no heap flag (OOMs → misleading exit per memory `reference_pulse_tsc_oom`) and no baseline (the 1,240 errors would hard-fail it), making the check meaningless. **Built the no-NEW-errors gate (AC1) + burndown doc (AC2)** — deliberately did **not** touch the 1,240 baseline errors (out of scope, risky; the gate is purely additive). Shipped: (1) **`scripts/tsc-baseline.mjs`** — self-contained ESM, **no new deps**, `spawnSync` (explicit argv, `shell:false`) launches a full cache-free tsc (`--incremental false --pretty false`) with `NODE_OPTIONS=--max-old-space-size=8192` injected into the *child* env so it never OOMs and is cross-platform **without** cross-env; error signature = `relPosixPath::TScode::normalizedMessage` (line/col **excluded** so unrelated edits that shift lines don't trip false "new error" alarms; path POSIX-normalized for Win-dev/Linux-CI parity); per-signature **counts** catch true regressions; `--update` writes the baseline, default mode gates (exit 1 listing each new error with real `file:line:col`, also reports FIXED signatures so the baseline can be tightened). (2) **`tsc-baseline.json`** — frozen baseline, **1,240 errors / 583 signatures**, sorted/deterministic. (3) **`package.json`** — `typecheck` (human full check via cross-env), `typecheck:baseline` (regenerate after fixing), `typecheck:gate` (CI gate). (4) **`ci.yml` + `tests.yml`** — both bare `npx tsc --noEmit` → `npm run typecheck:gate`. (5) **`.gitignore`** — allowlist `docs/*_BURNDOWN.md` (necessary: `docs/*.md` is ignored with a naming allowlist; chose the convention-consistent negation, matching `*_GUIDE.md`/`*_RUNBOOK.md`, over `git add -f`). (6) **`docs/TS_ERROR_BURNDOWN.md`** (AC2) — baseline total, top error codes (TS2304×338 missing-name/import, TS2339×265 prop-not-exist, TS2593×136 test-globals, TS2322×121, TS2307×111…), top files (`Messages/__tests__/ContextMenu.test.tsx`×106, `MessagesSplitView.test.tsx`×92, `RadialMenu.test.tsx`×88, `MessagesFeaturePanels.tsx`×72…), gate mechanics, contributor workflow (run `typecheck` → fix → `typecheck:baseline` → commit), prioritized burndown strategy (the test-globals/Vitest-types cluster is the highest-leverage first target). **Verified by the orchestrator:** ran the gate independently → `Baseline: 1240 · Current: 1240 · New: 0` → exit 0; the worker confirmed an injected scratch type error → `New: 1` / exit 1 with precise location, then clean again after removal (no trace). Clean explicit-path commit (7 files), gitleaks clean, `git pull --rebase` was already up to date, clean push. `_shots/` (pre-existing untracked screenshot scratch, not authored this session) left untouched throughout. **Honest note:** ~1,240 vs the issue's ~1,234 estimate is baseline drift since the estimate; the gate freezes the true current set. **Follow-up (not this run):** the actual burndown of the 1,240 (start with the test-globals cluster) is left to future PRs — the gate now guarantees the number can only go down. **Portability fix `948b8a3` (during the #122 run):** the #122 gate re-run surfaced a defect in this baseline — tsc embeds the *absolute* repo path inside some messages (dynamic-import TS2322 `import("F:/pulse1/src/…")`), whose drive-letter case varies by Windows shell and is a totally different root on Linux CI (`/home/runner/work/…`); 36 signatures carried it, so the committed baseline would have spuriously reported ~36 "new errors" on the **first CI run**. Fixed `normalizeMessage` to strip the runtime repo root (either separator, case-insensitive) before signing; regenerated `tsc-baseline.json` (still 1,240 / 583, now **zero** absolute paths); gate re-verified New:0. The gate is now genuinely environment-independent.
- **Prior issue:** [#121](https://github.com/FatherSonOne/Pulse-1/issues/121) — **Meeting Analytics writer (follow-up to #106)**. **DONE + closed** (`a1be70e`). The Meeting Analytics surface read a legacy `meetings` table that **nothing writes** (#106 made it honest; #121 makes it functional). The issue offered two options; **orchestrator chose Option 1 (native)** after verifying ground truth: native Pulse meetings persist to `pulse_video_rooms` with `duration_seconds`/`status='ended'` (`pulseVideoService.markRoomEnded`) **and** a structured Gemini `summary` JSON (`daily-webhook` → `parseStructuredSummary` → `{aiSummary, keyPoints, actionItems, decisions, topics, sentiment}`), so count/duration/trend/sentiment/topics/decisions are all genuinely native — Option 1 is fully Claude-verifiable against the real schema and works for every user with no Entomate dependency (Option 2 would only work Entomate-connected + can't be verified without live Entomate). **Shipped (3 files):** `meetingService.ts` `fetchMeetingAnalytics` repointed `.from('meetings')`→`.from('pulse_video_rooms')` (`created_by` + `status='ended'` = the honest "a meeting occurred" filter, matching `getMeetingRecordings`), `duration_seconds`→minutes, new `parseSummary` defensively `JSON.parse`es `summary` per row (neutral/empty fallback on null/plain-text/malformed — count the meeting, never fabricate signal), `topAttendees:[]`; `MeetingsComponents.tsx` removed the now-false `!entomateConnected` hard-gate + dead state (analytics render for any user ≥1 ended meeting), honest empty copy, People-tab explicit "attendees not native" empty state; `Meetings.tsx` ungated `MeetingsRailKicker` (native counts real; self-hides at 0) + removed dead state/import. **No regression to the #106 honesty gate:** the native sentiment/topics/decisions claim is now TRUE (server-side Gemini), so removing the gate is the upgrade #121 asked for, not a false 'native' claim. No coral (chrome stays on `--mtg-*`), no client-side AI, other `isEntomateConnected` call sites untouched, tsc no-new-errors on all 3 files (verified full `tsc --noEmit` grep-filtered → clean). **Clean run: only the 3 intended files touched** (no stray worker edits this time — the #123-run lesson held), explicit-path commit, gitleaks clean, push was a clean fast-forward (a parallel session had advanced main to `614a833`; no interference). **Honest gaps recorded:** (1) `topAttendees` never native — `pulse_video_rooms` has no attendees column; People tab is permanently an honest empty state until a participant store exists; (2) rooms with null/plain-text `summary` count toward totals/duration/trend but contribute neutral sentiment + no topics/decisions (a meeting that ended before `daily-webhook` ran slightly inflates the neutral tally — acceptable + honest); (3) the legacy `meetings` table is still read by `getMeetingRecordings`/`getMeetingRecordingById` (separate recordings feature) — left intact.
- **Prior issue:** [#123](https://github.com/FatherSonOne/Pulse-1/issues/123) — **Add `workspace_id` to the `Message Sent` trunk event**. **DONE + closed** (`42017f1`). Small clean code win that unblocks the NSM "Weekly Active Collaborative Workspaces" GROUP BY #118 deferred. **Issue premise was wrong and the worker correctly overturned it:** the issue claimed `currentWorkspace.id` was "in scope at the send moment" at `pulseService.ts:390` — it is not. `sendMessage(recipientId, content, contentType, mediaUrl)` is a **1:1 Pulse DM** via the `send_pulse_message` RPC, which writes a `pulse_messages` row with **no workspace/org column** (verified in migration `20260119062007_remote_schema.sql:3699`). A DM is genuinely workspace-less at the data layer, and there's no separate org id (Pulse models org == workspace per the `Workspace` type). **Resolved at the client layer:** added typed optional `workspace_id`/`organization_id` to `trackMessageSent` (`analytics.ts:247-269`, both forwarded via the existing `...properties` spread), and wired the emit site to read `getCurrentWorkspaceId()` (`src/services/ai/getWorkspaceId.ts`) — a **synchronous** read of the `pulse_active_workspace` localStorage key that `WorkspaceContext` maintains (set on init/selection at `WorkspaceContext.tsx:187/220/358…`, removed when none; the service-reachable mirror of `currentWorkspace.id`, same pattern AI services already use outside React). **No DB round-trip in the hot send path; stays inside the existing analytics `try/catch`** so it can never break the send. **Truth-in-product:** `workspace_id` is the sender's *active* workspace (best-effort), **omitted entirely when unset — never faked**; `organization_id` left unattached (no real source). tsc no-new-errors (only the pre-existing `analytics.ts:181` `people.increment` TS2339, present verbatim at HEAD). **Orchestrator caught + reverted a stray worker edit** — the worker had silently flipped `live-smoke-99 pending→in-progress` in `.claude/commands/launch-followups.md` (unrelated to #123 + factually wrong); `git checkout --` on that one file (this session's own erroneous edit), committed only the two #123 files with explicit paths. Clean push, gitleaks clean, no parallel-session interference (`git pull` was already up to date). **Honest gaps recorded:** (1) the id is the active workspace, not a per-message property — exact NSM attribution wants the deferred Supabase cron join (GUIDE §5.6); (2) Channel/Broadcast/Glimpse sends use their own RPCs and don't emit `Message Sent` at all (§5.4), so NSM from this event undercounts.
- **Prior issue:** [#124](https://github.com/FatherSonOne/Pulse-1/issues/124) — **Public-copy truth-in-product** (README + landing FAQ advertised non-real features). **DONE + closed** (`20283c5` landing + `2bc40a5` README; README body was applied earlier in `ece9a15`). This run finished the landing remainder unblocked by the #113 decision: in `src/components/LandingPage/landingData.ts` — (1) AI-vendor FAQ reconciled to **Gemini + Claude + OpenAI** (dropped ElevenLabs/Whisper/AssemblyAI, which #112 removed from Privacy §5 as "zero server-side evidence"); (2) the encryption FAQ's *"Support staff cannot read your message content"* **overclaim** replaced with the honest **#113 Option-B** wording (TLS + AES-256 + RLS; server-side AI on opted-in content; **no E2EE** — no cryptographic barrier; verbatim from `PULSE_E2EE_POSITIONING_GUIDE.md`); (3) **SMS** stripped from the "What is Pulse?" surface list + both pricing tiers' feature/cap lines (`inAppSms` is OFF #100, `smsService.isMockMode → true`); (4) 3 internal reference comments corrected (5 surfaces, 3 AI providers). README's stale "(Positioning tracked in #113)" hedge repointed to the settled guide. **Verified** via cross-surface grep sweep: no public claim contradicts the Privacy Policy / `package.json` / an OFF flag (remaining "end-to-end" hits are honest negations + Playwright "e2e"). Pure copy/data edits → tsc shows no `landingData` errors. Clean explicit-path commits, gitleaks clean, no parallel-session interference (a parallel session pushed RLS/search_path/search commits before this run — pulled, my #113 work intact). **Follow-up (NOT public copy, out of #124 scope):** quarantine the orphaned + buggy `src/services/encryption.ts` (dead libsodium `crypto_box`, 0 callers) so it isn't misread as real E2EE → **#104** umbrella.
- **Prior issue:** [#113](https://github.com/FatherSonOne/Pulse-1/issues/113) — **E2EE positioning decision**. **DONE + closed** (`dd9c5f4`, decision doc `docs/PULSE_E2EE_POSITIONING_GUIDE.md`). A decision, not code → drafted options + recommendation, operator approved **Option B**. Verified the actual crypto ground truth in-repo before drafting: **Pulse has no functioning E2EE** — `src/services/encryption.ts` is an orphaned libsodium `crypto_box` scaffold (repo-wide search → **0 callers**; also buggy, line 63 derives the "public key" by string-slicing the *secret* key). Messages are stored plaintext (RLS + AES-256-at-rest); the server-side `ai-router` reads plaintext for opted-in AI features (the core differentiator). So E2EE-for-everything (Option A) and server-side-AI-on-content are mutually exclusive — A was wrong-product, C (opt-in E2EE conversations) was logged as a post-launch candidate, **B** (honest "encrypted in transit + at rest; server-side AI on opted-in content; no E2EE") was the recommendation and the operator's choice. B simply ratifies the placeholder wording already used across `README.md:43-45/:120-124`, `PULSE_MARKETING_COPY_GUIDE.md:45`, and the `PULSE_README_HONEST_DRAFT_HANDOFF_2026-05-29.md`. The guide carries the **canonical approved wording** (use verbatim) + the AC mapping. **This unblocks #124** (the `priority: high` public-copy issue was `Depends-on #113`). **One residual contradiction surfaced + handed to #124:** landing FAQ `src/components/LandingPage/landingData.ts:48` says *"Support staff cannot read your message content"* — an overclaim with no E2EE (no cryptographic barrier; it's policy + access-control, not math); canonical replacement wording is in the guide. **Truth-in-product follow-up (not this run):** quarantine/delete the orphaned `encryption.ts` so it isn't mistaken for a real feature (#104/#124). Decision-only run — **no code changed**, clean explicit-path commit, gitleaks clean, no parallel-session interference.
- **Prior issue:** [#118](https://github.com/FatherSonOne/Pulse-1/issues/118) — Team/multiplayer activation onboarding (first-30-days). **DONE + closed** (`2627f6b`). Recognized as design-heavy → recon-mapped the existing onboarding/activation/invite/empty-state surfaces first, drafted a scoped v1, and the user signed off on the full 3-AC slice (member-count proxy for the depth signal). Shipped all three ACs, reuse-based, **no new backend plumbing**: **AC1** — `OrgSetupChecklist` reordered to lead with "Invite your team" + rose "Start here" emphasis so onboarding pushes invite → first multiplayer early; **AC2** — new deterministic `invite-teammate` nudge in `pulseNudgesService` (fires only when `canManageMembers && members.length < 2 && workspace created < 30d ago`; caller-supplied member/permission context, no extra query; priority 55), rendered via `PulseNudgesWidget` (also fixed two real gaps there: the widget never filtered `dismissedNudgesStorage`, and it stamped the AI-provenance chip on what is deterministic chrome — chip now gated to AI nudges only); **AC3** — "Invite a teammate" CTA added to the Relay Direct ("No messages yet") + Broadcast ("No broadcasts yet") + Decisions ("You're caught up") empty states, each gated `members.length < 2 && canManageMembers` (so already-formed teams aren't nagged; the private voice-notes empty state left solo by design). New `src/utils/inviteTeammateNavigation.ts` is the single routing backbone (writes `?settings=team&focus=invite` + dispatches `pulse:navigate`/`pulse:settings-navigate`, fires `OnboardingEvent.ChoreClicked` with a source tag; the real `TeammateInvited` funnel event still fires inside `inviteService`, not duplicated). **Design constraints honored:** all activation chrome on `--pulse-rose*`, zero coral (coral = AI-output only), no `AIProvenanceChip` on any deterministic surface. **Verified:** tsc (8GB heap) shows no new errors on the 9 touched files — every error in those files confirmed present verbatim at HEAD (`OrgSetupChecklist` Icon-style render @238, `ClassicMode` `bookmarked` @913/945/949, `PulseRadio` `Tower` import @30, `LiveDashboard` untouched). Clean explicit-path commit, gitleaks clean, no parallel-session interference. **Deferred (clearly out of this scope):** the full collaborative-depth meter using the true NSM ≥50-weekly-messages threshold is blocked on #123 (`workspace_id` on `Message Sent`); the post-Stripe onboarding redesign surface (which would light up the 5 dead `OnboardingEvent`s) remains unbuilt; more empty states could route to invite later.
- **Prior issue:** [#125](https://github.com/FatherSonOne/Pulse-1/issues/125) — Reconcile Decisions "voting". **DONE + closed** (`5f72bcd`). Determination overturned the review's framing (R-02): end-user decision voting is **real, DB-backed, and ungated**, not gated by the OFF `proposalMode` flag. Traced `DecisionDetail.handleVote` + `CockpitHub` quick-approve → `decisionService.castVote()` → upsert into `public.decision_votes`; **verified in the live prod DB** (`ucaeuszgoihoyrvhewxk`): the table exists with columns matching the `DecisionVote` interface, the `UNIQUE(decision_id, user_id)` constraint the upsert's `onConflict` relies on is present, RLS is enabled (1 policy), and there are real vote rows (1 vote across 15 decisions). The Cockpit has been the *only* Decisions surface since Phase 12 (`App.tsx` renders `CockpitHub` directly), so the approve/reject/concern/abstain UI ships to all users. `proposalMode` is a **separate** feature — the in-Messages composer "proposal mode" (Ctrl+Shift+P) that attaches `decisionData:{type:'proposal'}` to a chat message with client-side **simulated** votes that never touch `decision_votes`; its description `'Proposal-mode decision capture and voting'` is what misled the review into thinking *all* voting was off. **Fix (truth-in-product, claim was TRUE):** rewrote the misleading `proposalMode` comment + `description` in `featureFlags.ts` to disambiguate it from the real Decisions voting; restored the Capability Matrix row from ✅/🟡 back to ✅ with the distinction spelled out; relaxed the `PULSE_MARKETING_COPY_GUIDE.md` "voting stays out of headline" note now that voting is confirmed real (with a caveat to not market the OFF Messages proposal mode). Landing page's "Voting" demo lane (`3 / 4 voted`) is truthful. Comment/string-only code change → no TS impact. Clean run; only-my-files committed with explicit paths (a parallel session's `teamRadarService.ts` WIP was present at session start, confirmed by the user as held WIP and left untouched).
- **Prior issue:** [#117](https://github.com/FatherSonOne/Pulse-1/issues/117) — Retention instrumentation + define the NSM. **DONE + closed** (`a3d3cf7`). Investigation overturned the issue's framing: PostHog + Sentry were in deps and `src/lib/monitoring/` already defined ~14 typed event helpers + the `OnboardingEvent` enum + `surface_shown`/`named`/`checklist_opened`/`checkout_completed` emit sites, but the whole stack was **dormant in production**: `initializeMonitoring()` had zero callers anywhere in `src/`, `identifyUser()` likewise never invoked, and the documented "moment of truth" `OnboardingEvent.FirstMessageSent` was defined but never emitted (Messages.tsx:2451 fires a *different* in-app `triggerMessage` system — verified via grep, not PostHog). So zero events left the browser; D1/D7/D30 cohorts impossible. **Fixed:** bootstrap `initializeMonitoring()` from `main.tsx:23-32` inside the `!E2E_MAP_HARNESS` guard with try/catch; `identifyUser(session.user.id, ...)` on `SIGNED_IN` + `resetAnalytics()` on `SIGNED_OUT` wired into the existing `onAuthStateChange` callback in `AuthContext.tsx:106-122` (no extra round-trip — uses only `User` fields already in scope); trunk `trackMessageSent()` call placed **once** in `pulseService.sendMessage` after RPC success (all Pulse DM modes funnel through it — per-mode spray deliberately avoided per issue note); `OnboardingEvent.FirstMessageSent` emit added **alongside** the existing in-app `triggerMessage` (both must fire, different systems); `OnboardingEvent.TeammateInvited` fires on both `inviteService.ts` Resend (:144) and Gmail (:197) paths with a `channel` discriminator; stale autocapture URL allowlist `[/pulsemessages\.com/]` → `[/pulse\.logosvision\.org/, /localhost/]`. **NSM defined:** `docs/PULSE_NSM_AND_RETENTION_GUIDE.md` — **Weekly Active Collaborative Workspaces** = workspaces where, in the trailing 7 days, ≥2 distinct members were active AND ≥50 messages were sent (anchored in the Slack ~2,000-message → 93% retention finding from audit point 3; solo workspaces explicitly excluded). Doc also covers DAU/MAU stickiness, D1/D7/D30 cohort definitions, the activation funnel, PostHog Insight rebuild instructions, aha-event reference table with file:line, and known gaps. **Verified:** `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit` → no new errors on the six touched src/ files (single pre-existing `analytics.ts:181 posthog.people.increment` type mismatch in `incrementUserProperty` is NOT in this diff). Clean commit, no parallel-session interference this run.
- **Last issue worked:** [#99](https://github.com/FatherSonOne/Pulse-1/issues/99) — **CRM OAuth callback server-side refactor**. **Code piece DONE** (`5ed5f94`); issue kept **open** for the human OAuth smoke. Diagnosed the `/api/crm/callback/:platform` failure as two compounding bugs: `server.js` runs as plain `node` (`"type":"module"`, no TS transpile) yet imported `oauthHelper.**ts**` as `.js` (unresolvable), and `oauthHelper.ts` imports the browser-only `{ supabase } from '../supabase'` (`supabase.ts` reads `import.meta.env` → undefined in Node, + Capacitor libs). The route's actual call — `exchangeCodeForToken` — is a **pure provider `fetch`** with no Supabase (the route redirects tokens to the frontend via base64 params, which persists them). **Fix:** new `server/crmOAuth.js` — plain-ESM Node-safe mirror of the browser-free `CRM_OAUTH_CONFIGS` + `exchangeCodeForToken` (verbatim from the `.ts` + an unknown-platform guard); repointed the route's dynamic import; keep-in-sync NOTE added to `oauthHelper.ts`. **Verified without live creds:** `node -e import('./server/crmOAuth.js')` loads clean (exports + 4 platforms), `node --check server.js` passes, tsc no-new-errors on the comment-only `.ts` edit. **Deliberately NOT done (handed to #108):** the second broken import (`server.js:911`, `crmService.fullSync` — the full browser sync engine) is a much larger Node port overlapping #108's pagination work; porting it here would be unverifiable without provider creds and out of #99's "make the callback work" scope. **#108's dependency on #99 is now satisfied for the connect path** (you can OAuth-connect a CRM) — #108 inherits the sync-engine port + pagination. Clean explicit-path commit (3 files: new `server/crmOAuth.js`, `server.js`, `oauthHelper.ts`), gitleaks clean, no parallel-session interference.
- **SMS strategy (2026-05-30, decided):** Operator confirmed **SMS is a v1.1 item — defer** (`docs/PULSE_SMS_STRATEGY_GUIDE.md`). The review's SMS pressure (R-03) was *advertising a fake feature*, already resolved by #100 (hidden) + #124 (de-copied) — not by shipping SMS. #120 (real wiring) is the *last* step, blocked behind the #109 10DLC regulatory gate (weeks of carrier vetting, no code shortcut), so building it pre-launch = rot. #109/#120 stay backlogged; 10DLC clock **not** started. Codebase note for later: the **mock in-app sender** (`smsService.ts`) is separate from the **real `twilioService` → Unified Inbox read path** (the compliance-lightest entry if an inbound-only slice is ever wanted). Not a code run — no issue closed.
- **Last run (date):** 2026-05-31 — **#110 + #119 both DECIDED + CLOSED same session.** #110 (`9c89b56`): CALEA N/A for v1, ship on eng read, tripwire stands. #119 (`80c4e6b`→`d90d35a`): Lane A solo-first + pricing locked — **Solo $20/mo, Team per-seat $15/seat (min 2), Growth $300**. Two follow-ups need their own issues (landing-copy swap; Stripe pricing implementation). Drafts `80c4e6b`/`2515b4f`; pricing `9c89b56`/`d90d35a`. Prior: 2026-05-31 — #122 Tier-2 "Claude watched your inbox" AI digest, flag-gated OFF (`5223a18`; 2 human/visual items remain; operator must `supabase functions deploy ai-router` before enabling). Prior: 2026-05-31 — #122 Glimpse dead-code cleanup (`38254b6`) + #114 baseline portability fix (`948b8a3`). Prior: 2026-05-31 — #114 TS no-new-errors CI gate + burndown doc (`a249248`; done + closed). Prior: 2026-05-31 — #116 observability/SLOs code slice (`4739d89`; code done, operator-infra remaining); #121 native Meeting Analytics from `pulse_video_rooms` (`a1be70e`; done + closed); #123 `workspace_id` on `Message Sent` (`42017f1`; done); 2026-05-30 — #99 CRM OAuth callback server-side refactor (`5ed5f94`; code done, issue kept open for the human OAuth smoke + #108 hand-off); SMS strategy decision (defer to v1.1); #124 (closed); #113 (closed; E2EE → Option B); #118 (closed; team activation); #125 (closed); 2026-05-27 — #117 (closed). **The #119 pricing decision spawned two new follow-up issues (`2134ffd`): [#126](https://github.com/FatherSonOne/Pulse-1/issues/126) (pricing implementation, `priority: high`) + [#127](https://github.com/FatherSonOne/Pulse-1/issues/127) (Lane-A landing copy, `medium`).** #126 is now the highest-priority open item — it has a real Claude-buildable portion (plans migration + Solo caps + checkout `quantity` fix + Solo billing UI) but is **operator-gated on the Stripe step first** (operator runs `scripts/setup-pulse-pricing-2026.mjs` with their key to mint the price IDs Claude needs for the migration).
- **Next up — pick from the unblocked `medium` code queue.** With #114 + #116(code) + #121 + #123 done, the remaining Claude-executable code is thin:
  - **#122** (Glimpse Path D follow-ups) — **cleanup (`38254b6`) + Tier-2 AI digest (`5223a18`) both DONE**. Remaining 2 items are **human/visual only, no Claude code slice**: the **visual pass** (`_design-playground/_shots/glf-01..04`, now incl. the Tier-2 ready/loading states) + the **e2e a11y spec** (needs a live auth-fixture refresh). Route via `/launch-followups`. **Also a fresh operator handoff:** `supabase functions deploy ai-router` to activate the `glimpse_inbox_digest` task before the `glimpseInboxDigest` flag can be flipped on (safe-but-inert until then).
  - **#126** (pricing implementation, **`priority: high`** — NEW from the #119 decision) — **the new top item.** Two-phase: **(operator first)** run `scripts/setup-pulse-pricing-2026.mjs` (scaffolded, `2134ffd`) with the Stripe key in TEST to mint the Solo + per-seat-Team price IDs; **(then Claude)** add the `pulse_solo` plans row + Solo entitlement caps, repoint `pulse_team` at the per-seat price, fix `billing-checkout`'s hardcoded `quantity:1` → member count for per-seat Team, add Solo billing UI. Per-seat sync/reconcile already exist. Money-touching → TEST → smoke → LIVE.
  - **#127** (Lane-A landing copy, `medium` — NEW) — Claude-buildable (`landingData.ts` + marketing copy guide), **sequence after #126's TEST setup** so displayed prices match real checkout.
  - **#119** + **#110** — **DECIDED + CLOSED** this session (Lane A + pricing; CALEA N/A). No longer in the queue.
  - **#108** (CRM sync — `crmService.fullSync` Node port + pagination) — needs live provider creds to verify, so partially human-gated. Its **pagination half can be written blind** if a code run is wanted; the sync-engine Node port + live verify still need creds.
  - **Human-gated (route via `/launch-followups`, not here):** **#116** remainder (uptime/status-page signup + Sentry/Supabase alert rules — operator infra); **#115** (integration test coverage — QA setup); **#99** remainder (OAuth round-trip smoke); **#104** (close once the #115 click-through passes — do NOT re-pick for code); **#109/#120** (SMS, deferred to v1.1, 10DLC clock not started); **#122** Tier-2 (`supabase functions deploy ai-router` + flag flip) + its visual/e2e pass.
  - **Recommendation:** **#126 (pricing implementation) is now the top priority** — but its first step is the operator running the scaffolded Stripe script to mint price IDs; once those exist, Claude does the plans migration + checkout `quantity` fix + Solo billing UI, then #127 (landing copy). If the operator hasn't run the Stripe step yet, the next *unblocked* Claude code is **#108**'s pagination half (blind-writable; full verify needs live CRM creds). Everything else is operator/human via `/launch-followups` (ai-router deploy + flag flips, OAuth/integration smokes, QA click-throughs, region var).
  - **#99 is no longer a code pick** — its remaining work is the human OAuth round-trip smoke (register CRM redirect URIs + creds on Render, connect a CRM) + the LV `localhost:5176` redirect. Route via `/launch-followups`.
- **Open blockers / decisions waiting on the human:**
  - **#112 — co-commit honesty.** Commit `a4d5006` is labeled Glimpse-only but contains the entire #112 implementation (7 files). The diff content is correct; the headline is not. Disclosed on issue + here; no force-push to fix per CLAUDE.md. If future audit needs a cleaner split, the only safe route is squashing in a future merge or annotating CHANGELOG.
  - **#111 — set `VITE_SUPABASE_REGION="US East (us-east-1)"` in Vercel** (production + preview) so Settings → Compliance shows the real region instead of the "Multi-region" fallback. The var is wired + documented; only the Vercel value is pending. A full live account-deletion test needs a **throwaway account** (the op is irreversible + `auth.uid()`-guarded). Export-completeness gap (contacts/calendar/email categories return empty in the legacy `dataExportService`) documented in the runbook §8 as a separate follow-up.
  - **#102 email — CLOSED 2026-05-26.** DMARC record set by operator; all acceptance items met. (Residual browser-key-exposure hardening = route invite/RSVP through the `send-email` edge fn, folded into the #104/#105 truth-in-product work.)
  - **#104 product calls (3) — ALL RESOLVED 2026-05-26.** (a) Email→Campaigns → **hidden** (#105, `0767141`). (b) Map → **leave in Experimental, circle back** (may not make v1, don't abandon). (c) `MESSAGE_ANALYTICS` → **wired into nav** (`3d51235`). Recorded in `docs/FEATURE_FLAG_AUDIT_v1.md`.
  - **#99 backend — DEPLOYED** on Render (`pulse-api-1epw.onrender.com`) + `VITE_BACKEND_URL` wired in Vercel. Remaining: (a) **user-driven live round-trip smoke** (connect Slack / load channels / Gmail refresh from `pulse.logosvision.org`); (b) **CRM OAuth server-side refactor** — `oauthHelper`/`crmService` import the browser-only `supabase` client, so `/api/crm/callback` can't run server-side (separate follow-up — unblocks #108); (c) LV contacts-import Google flow hardcodes `localhost:5176` redirect.
- **Notes for next run:** Re-read the Status Table first. **All P0 code shipped. P1 truth-in-product + GDPR + ToS swept. P2 retention (#117) + team activation (#118) + `workspace_id` analytics dimension (#123) + native Meeting Analytics (#121) shipped.** The launch-blocker triad is closed. **As of 2026-05-31 the P2 code queue is swept: #114 (CI no-new-errors gate, `a249248` + portability fix `948b8a3`), #116 code slice (`4739d89`), #121 (`a1be70e`), #122 cleanup (`38254b6`) + Tier-2 digest (`5223a18`), #123 (`42017f1`) all shipped.** **No unblocked Claude-executable code remains, and the two decision items #119 (positioning → Lane A solo-first) + #110 (CALEA → N/A for v1) are now DRAFTED + awaiting operator sign-off** (`80c4e6b`/`2515b4f`). The Claude-actionable surface of the roadmap is effectively exhausted; the ball is in the operator's court. Everything left is operator/human (route via `/launch-followups`): ratify #119 + #110; `supabase functions deploy ai-router` (activates #122 Tier-2's `glimpse_inbox_digest` task before the flag can flip) + #116 alert-rule/status-page infra + #111 region var + #99 OAuth smoke + #104/#115 QA. The only remaining *code* pick is #108's pagination half (blind-writable; full verify needs live CRM creds). **Worker-agent hygiene (held this run, bit the #123 run):** always `git diff --stat` the FULL working tree, not just the files the worker reports — the #123 worker silently edited an unrelated file; the #121 worker was clean (only the 3 intended files). Commit only the issue's files with explicit paths. **#121 native-analytics reuse note:** `pulse_video_rooms` is the canonical native meeting store (`created_by`/`status='ended'`/`duration_seconds`/`summary` JSON via `daily-webhook`→`parseStructuredSummary`); the legacy `meetings` table is orphaned for analytics but still feeds `getMeetingRecordings`/`getMeetingRecordingById`. Attendees are not captured anywhere native yet (a participant store would unblock the People tab). **NSM note:** #123 attaches the sender's *active* workspace to `Message Sent` (best-effort, omitted when unset). The precise NSM ≥50-weekly-messages collaborative-depth signal #118 deferred still needs the Supabase cron/materialized-view join (GUIDE §5.1/§5.6) — a separate follow-up, not yet filed as its own issue. **#118 reuse map for any follow-on onboarding work:** the activation backbone is now `src/utils/inviteTeammateNavigation.ts` (routes to Settings→Team→invite), the deterministic nudge host is `pulseNudgesService.getPulseNudges` + `PulseNudgesWidget` (NOTE: the widget now filters `dismissedNudgesStorage` and gates the AI chip to AI-provider nudges — keep new deterministic nudges off the chip), and the onboarding-checklist scaffold is `OrgSetupChecklist` (now leads with invite). `trackOnboarding(OnboardingEvent.*)` (`src/lib/monitoring/onboardingEvents.ts`) is the funnel emit primitive; **5 enum events remain dead** (PlanPickerNamed, FounderNotePlayed, FirstCaptureMade, PolishOpened, PolishItemCompleted) tied to the unbuilt post-Stripe redesign. **Design constraint that bit this run and will bite again:** activation/onboarding/nudge chrome is deterministic, so it stays on `--pulse-rose*` and must NOT carry coral or the `AIProvenanceChip` (coral = AI-output only). **Parallel-session hygiene:** always `git status` at start; explicit-path `git add`, never bare `git commit`, never stash/pop (memory `avoid_stash_with_parallel_sessions`). The `teamRadarService.ts` WIP flagged at the #125 run is gone (committed by a parallel session). Doc-tracking: `docs/*.md` is gitignored but `*_RUNBOOK.md`/`*_ROADMAP.md`/`*_GUIDE.md`/`*_HANDOFF_*`/`*_AUDIT_*` are allowlisted; other doc names need `git add -f`. **Done sweep:** P0 (#99 deferred-remainder / #100 / #101 / #102 / #103); P1 truth-in-product (#100/#101/#102/#103/#105/#106/#107/#125); P1 GDPR+ToS (#111/#112); P2 (#117 retention, #118 activation). **Do NOT re-pick #104** (umbrella; only the #115 human QA click-through remains).

---

## Changelog

- **2026-05-31** — **#110 + #119 both DECIDED + CLOSED** (`9c89b56`/`d90d35a`). #110: CALEA N/A for v1, shipping on the engineering determination (no lawyer), tripwire stands. #119: Lane A confirmed (solo-first); operator flagged $100/$300 as wrong for solo → built `docs/PULSE_PRICING_UNIT_ECONOMICS_GUIDE.md` (per-solo COGS ~$3–6/mo, AI cap ~$8 worst-case, break-even ~5 Solo users) → **pricing locked: Pulse Solo $20/mo (new hero) + Team per-seat $15/seat (min 2) + Growth $300 unchanged.** Filed the two follow-ups (`2134ffd`): **[#126](https://github.com/FatherSonOne/Pulse-1/issues/126)** pricing implementation (`priority: high`) + **[#127](https://github.com/FatherSonOne/Pulse-1/issues/127)** Lane-A landing copy (`medium`), and scaffolded `scripts/setup-pulse-pricing-2026.mjs` (Stripe Solo product + per-seat Team prices — operator runs it). Per-seat sync/reconcile already exist; checkout's hardcoded `quantity:1` is the main gap. `[EST]` vendor rates still to verify vs invoices.
- **2026-05-31** — **#119 + #110 decision drafts** (operator asked for both in one run; both OPEN awaiting sign-off). **#119 positioning** (`80c4e6b`, `docs/PULSE_POSITIONING_GUIDE.md`): lead = cross-surface AI over one data model + on-every-line provenance; recommends **Lane A solo-first** (cross-surface AI hub for the overloaded solo operator + team as on-ramp), resolving the R-07 PRODUCT.md-solo vs landing-"teams" split; B/C variants + ranked differentiators. **#110 CALEA** (`2515b4f`, `docs/PULSE_CALEA_DETERMINATION_GUIDE.md`): engineering determination (not legal advice) — Pulse voice/video fails the FCC interconnected-VoIP test (no PSTN interconnection: async app-to-app Relay/Glimpse + WebRTC Daily) → **CALEA N/A for v1**, no SSI/intercept build; recommends one-time lawyer confirm + a PSTN tripwire. Both commented with the recommendation + explicit operator question; neither closed. Doc-only commits, gitleaks clean. **This exhausts the Claude-actionable roadmap surface** — remaining work is operator decisions + `/launch-followups` items.
- **2026-05-31** — #122 Glimpse **Tier-2 "Claude watched your inbox" AI digest** (`5223a18`; flag-gated OFF, issue stays open — 2 human/visual items remain). Real server-side cross-conversation synthesis layered over the always-on Tier-1 deterministic briefing. Extended `ai-router` with a `glimpse_inbox_digest` task (Claude Haiku + Gemini fallback) rather than a standalone fn — client builds the prompt from data it holds + calls `invokeAIJson` (model call server-side, no React SDK). New `useGlimpseInboxDigest` hook (gates to idle when flag-off/no-needs/no-workspace; abort + signature-dedupe; any throw → Tier-1); `GlimpseBriefingCard` shows the Claude headline + muted provenance chip + clickable highlights when ready, pending chip when loading, pure Tier-1 otherwise; deterministic counts + needs list always render (truth-in-product). Flag OFF + comment; tsc gate New:0. **⚠️ Operator: `supabase functions deploy ai-router` before flipping the flag** (un-redeployed → `invalid_task` → safe-but-inert Tier-1). The P2 Claude-code queue is now swept; next runs are decision drafts (#119/#110).
- **2026-05-31** — #122 Glimpse **dead-code cleanup** slice (`38254b6`; issue stays open — then 3 items remained). Removed the verified-obsolete pre-redesign Triage Cockpit row: unused `ConversationItem` component + the table-only `.gl-tc-*` CSS (rows/avatar/signal/summary/skeleton/counter + responsive collapse + 2 reduced-motion selectors). Net 7 ins / **382 del**. Surgical 3-way keep/delete (KEPT `.gl-tc-action-pill`/`-ai-chip` reused by live `ReelCard` + the `.gl-tc-empty*` empty state). Verified independently (kept refs survive, dead grep=0, CSS 445/445 balanced, tsc gate no-new-errors). Committed only my own files w/ explicit paths while a parallel session's auth-signup WIP (`98c43e3`/`8d583e7`) + privacy fix (`d99381a`) landed in the shared tree (user said leave it; my commits FF-stacked on top, nothing swept). Remaining #122: Tier-2 AI inbox-digest edge fn (build-for-v1 decision needed), visual pass, e2e a11y spec. **Also `948b8a3`:** portability fix to the #114 baseline gate — strip the env-specific absolute repo root tsc embeds in some messages (would've spuriously failed Linux CI on 36 signatures); regenerated baseline (1,240/583, zero abs paths), gate re-verified New:0.
- **2026-05-31** — #114 TypeScript no-NEW-errors CI gate + burndown doc **DONE + closed** (`a249248`). The type-safety net was off (Vite/esbuild skip type-checking → ~1,240 `tsc` errors accumulated; both CI tsc steps ran bare `npx tsc --noEmit` with no heap flag (OOM-false-pass per `reference_pulse_tsc_oom`) and no baseline → meaningless). Built the additive gate (AC1) without touching the 1,240 baseline errors: new dependency-free `scripts/tsc-baseline.mjs` (full cache-free tsc with 8GB heap injected into the child, cross-platform; signature `relPosixPath::TScode::normalizedMessage` excludes line/col so unrelated edits don't churn; per-signature counts catch regressions; `--update` writes / default-mode gates), frozen `tsc-baseline.json` (1,240 errors / 583 sigs), `typecheck`/`typecheck:baseline`/`typecheck:gate` npm scripts, both `ci.yml`+`tests.yml` repointed to `npm run typecheck:gate`, `.gitignore` allowlists `docs/*_BURNDOWN.md`. AC2 doc `docs/TS_ERROR_BURNDOWN.md` (top codes/files + burndown strategy; test-globals cluster = highest-leverage first target). Orchestrator re-verified gate New:0/exit0 on baseline; worker confirmed it catches an injected error (New:1/exit1) then clean after removal. Clean explicit-path commit (7 files), gitleaks clean, rebase up-to-date, clean push; `_shots/` untracked scratch left untouched. Next code run (if wanted) → #122 (Tier-2 AI digest, get operator sign-off first); else draft #119/#110.
- **2026-05-31** — #116 Observability + SLOs **code slice DONE** (`4739d89`; issue kept open for operator infra, same shape as #99/#101). New `docs/PULSE_OBSERVABILITY_RUNBOOK.md` (AC#3 ✅: v1 SLOs/SLIs for message-send/auth/AI-router with error budgets + how-measured) + Sentry instrumentation wired into the real trunk seams — `AuthContext.handleLogin` (auth SLI + `setUserContext`/`clearUserContext` co-located with #117's identify/reset), `pulseService.sendMessage` (message-send SLI), `aiService.invokeAI` (the single ai-router chokepoint all 29 geminiService exports delegate through) — using the defined-but-unused `src/lib/monitoring/sentry.ts` helpers. tsc 0-new in touched files; gitleaks clean; pushed clean (parallel session had advanced main to `ff96278`, rebased fine). AC#1 (status page) + AC#2 (alert rules) kept open as operator infra (runbook §6). **Correction:** a first draft this session (never committed, discarded by operator) mis-stated paths as `utils/sentry.ts`/`geminiService.callRouter` and wrongly claimed #117 never wired `identifyUser`/`resetAnalytics` in AuthContext — it did (`AuthContext.tsx:105`, from `lib/monitoring/analytics`). Reimplemented clean. Next up → #108.
- **2026-05-31** — #121 Meeting Analytics writer (follow-up to #106) **DONE + closed** (`a1be70e`). The surface read a legacy `meetings` table that nothing writes (#106 made it honest; #121 makes it functional). **Orchestrator chose Option 1 (native)** after verifying native meetings persist `duration_seconds`/`status='ended'` (`pulseVideoService.markRoomEnded`) + a structured Gemini `summary` JSON (`daily-webhook`→`parseStructuredSummary` → `{aiSummary,keyPoints,actionItems,decisions,topics,sentiment}`) to `pulse_video_rooms` — fully Claude-verifiable, no Entomate dependency (Option 2 would only work Entomate-connected + couldn't be verified headless). Repointed `fetchMeetingAnalytics` `meetings`→`pulse_video_rooms` (`created_by`+`status='ended'`), `duration_seconds`→min, defensive per-row `JSON.parse` of `summary` for sentiment/topics/decisions (neutral/empty fallback, never faked), `topAttendees:[]`; removed the now-false #106 Entomate hard-gate on `AnalyticsModal`+`MeetingsRailKicker` (native claim is now TRUE) with honest empty states preserved + a People-tab "attendees not native" empty. No coral (`--mtg-*` only), no client AI, other `isEntomateConnected` sites untouched, tsc no-new-errors on all 3 files. Clean run — only the 3 intended files touched (#123-run stray-edit lesson held), explicit-path commit, gitleaks clean, clean fast-forward push. Gaps: attendees never native (no column); null/plain-text summaries count toward totals but contribute only neutral sentiment.
- **2026-05-31** — #123 `workspace_id` on the `Message Sent` trunk event **DONE + closed** (`42017f1`). Worker overturned the issue premise: the emit site (`pulseService.sendMessage` → `send_pulse_message` RPC) is a 1:1 DM that writes a **workspace-less** `pulse_messages` row (no workspace/org column; verified in `20260119062007_remote_schema.sql:3699`), so `currentWorkspace.id` was never in scope. Fixed at the client layer: typed optional `workspace_id`/`organization_id` added to `trackMessageSent` (`analytics.ts:247-269`); emit reads `getCurrentWorkspaceId()` (synchronous `pulse_active_workspace` localStorage read — same key `WorkspaceContext` maintains; no DB round-trip; inside the analytics `try/catch`). Truth-in-product: best-effort *active* workspace, **omitted when unset, never faked**; `organization_id` unattached (org == workspace). tsc no-new-errors. Unblocks the PostHog GROUP BY; exact NSM attribution still wants the deferred Supabase cron join (GUIDE §5.6). **Orchestrator caught + reverted a stray worker edit** to `.claude/commands/launch-followups.md` (unrelated #99-status flip) before committing only the two #123 files with explicit paths. PostHog Live-events smoke = human verify.
- **2026-05-30** — #99 **CRM OAuth callback now functional server-side** (`5ed5f94`; issue kept open for the human smoke). The `/api/crm/callback/:platform` route threw on every call due to two compounding bugs: `server.js` runs as plain `node` (`"type":"module"`, no TS transpile) yet imported `oauthHelper.**ts**` as `.js`, and `oauthHelper.ts` imports the browser-only `{ supabase } from '../supabase'` (`import.meta.env` undefined in Node + Capacitor libs). Since the route's `exchangeCodeForToken` is a pure provider `fetch` (no Supabase — tokens redirect to the frontend via base64 params), the fix is a Node-safe `server/crmOAuth.js` mirroring the browser-free `CRM_OAUTH_CONFIGS` + `exchangeCodeForToken`; repointed the route's dynamic import; keep-in-sync NOTE in `oauthHelper.ts`. Verified `node` loads the module + `node --check server.js`, tsc no-new-errors. **Handed to #108:** the second broken import (`server.js:911`, `crmService.fullSync` — the full browser sync engine) needs a larger Node port that overlaps #108's pagination; not done here. #99's `critical` code blocker is resolved; only the human OAuth round-trip smoke + LV redirect remain.
- **2026-05-30** — **SMS strategy decision: defer to v1.1** (`docs/PULSE_SMS_STRATEGY_GUIDE.md`, `cd8b6fa`). Answered the strategic question "what part of the SMS gap benefits from being pursued now?" — answer: *none for v1*. The review's R-03 pressure (mocked SMS advertised in pricing/FAQ) was already resolved by #100 (surface hidden) + #124 (removed from copy); real sending (#120) is blocked behind the #109 10DLC regulatory gate (weeks of carrier vetting, no code shortcut) so building it now = rot. Mapped the two SMS surfaces (mock `smsService` sender vs real `twilioService` inbox feeder). Operator chose Option 1 (defer); #109/#120 annotated + stay backlogged, 10DLC clock not started. No code/issue changed.
- **2026-05-30** — #124 Public-copy truth-in-product **DONE + closed** (`20283c5` landing + `2bc40a5` README; README body earlier in `ece9a15`). Finished the landing remainder the #113 decision unblocked, all in `src/components/LandingPage/landingData.ts`: AI-vendor FAQ → **Gemini + Claude + OpenAI** (dropped ElevenLabs/Whisper/AssemblyAI per Privacy §5 / #112); encryption FAQ "support staff cannot read" **overclaim** → honest **#113 Option-B** wording (TLS + AES-256 + RLS, server-side AI on opted-in content, no E2EE); **SMS** stripped from the surface list + both pricing tiers (gated OFF via `inAppSms` #100); 3 reference comments corrected. README hedge "(Positioning tracked in #113)" → settled `PULSE_E2EE_POSITIONING_GUIDE.md`. Cross-surface grep sweep confirms no public claim now contradicts the Privacy Policy / `package.json` / an OFF flag. Pure copy/data edits, tsc clean on the file. **This closes the `priority: high` truth-in-product sweep** (#104 umbrella now needs only the #115 human QA + the orphaned-`encryption.ts` cleanup). Worker-agent implemented the exact specced edits; orchestrator reviewed the diff + committed. Parallel session had pushed RLS/search commits before this run — pulled clean, #113 work intact.
- **2026-05-30** — #113 E2EE positioning decision **DONE + closed** (`dd9c5f4`, `docs/PULSE_E2EE_POSITIONING_GUIDE.md`). A decision (draft-and-ask), not code. Verified in-repo ground truth first: **no functioning E2EE** — `src/services/encryption.ts` is an orphaned (0 callers) + buggy libsodium `crypto_box` scaffold; messages stored plaintext (RLS + AES-256-at-rest); server-side `ai-router` reads plaintext for opted-in AI (the differentiator). Laid out 3 options; **operator approved Option B** — honest "encrypted in transit (TLS) + at rest (AES-256) + RLS; server-side AI on opted-in content; **no E2EE**." Ratifies the placeholder already used across README/marketing-guide/README-handoff. Option A (real E2EE) = wrong product (forfeits the AI moat); Option C (opt-in E2EE conversations) = post-launch roadmap candidate, not marketed until shipped. Guide carries canonical verbatim wording + AC mapping. **Unblocks #124** (was `Depends-on #113`). Residual contradiction handed to #124: landing FAQ `landingData.ts:48` "support cannot read" overclaim + AI-vendor reconcile. Truth-in-product follow-up: quarantine the orphaned `encryption.ts`. Decision-only, no code changed; clean explicit-path commit, gitleaks clean.
- **2026-05-30** — #118 Team/multiplayer activation onboarding **DONE + closed** (`2627f6b`). Design-heavy → recon-first, scoped v1, user signed off on the full 3-AC slice (member-count proxy for depth). Reuse-based, no new backend: **AC1** `OrgSetupChecklist` leads with invite (+rose "Start here"); **AC2** new deterministic `invite-teammate` nudge in `pulseNudgesService` (`canManageMembers && members<2 && ws age <30d`; also fixed `PulseNudgesWidget` ignoring dismissals + stamping the AI chip on deterministic chrome); **AC3** "Invite a teammate" CTA in Relay Direct/Broadcast + Decisions "You're caught up" empty states (gated `members<2 && canManageMembers`). Shared `src/utils/inviteTeammateNavigation.ts` backbone. Rose-only, no coral, no AIProvenanceChip on activation surfaces. tsc no-new-errors (all 9 touched-file errors confirmed at HEAD). **Deferred:** true NSM ≥50-weekly-msg depth signal (blocked on #123), post-Stripe onboarding redesign (5 dead `OnboardingEvent`s), more empty states.
- **2026-05-30** — #125 Reconcile Decisions "voting" **DONE + closed**. Determination overturned review finding R-02: end-user decision voting is **real, DB-backed, ungated** — `DecisionDetail.handleVote` + `CockpitHub` quick-approve → `decisionService.castVote()` → `public.decision_votes` (verified live in prod `ucaeuszgoihoyrvhewxk`: table + `UNIQUE(decision_id,user_id)` + RLS + real vote rows; Cockpit is the only Decisions surface since Phase 12). The OFF `proposalMode` flag gates a *separate* in-Messages composer proposal mode with client-side **simulated** votes that never persist — its `description` ("Proposal-mode decision capture and voting") is what made the review think all voting was off. Truth-in-product fix (the claim was TRUE): disambiguated the `proposalMode` comment + `description` in `featureFlags.ts`, restored the Capability Matrix "Decisions + voting" row ✅/🟡 → ✅ with the distinction spelled out, and cleared the "voting" wording in `PULSE_MARKETING_COPY_GUIDE.md` (with a caveat not to market the OFF Messages proposal mode). Comment/string-only code edit → no TS impact. Clean run, explicit-path commit; a parallel session's uncommitted `teamRadarService.ts` was present at session start and left untouched per the user (held WIP).
- **2026-05-25** — Roadmap created; audit captured; labels `launch-roadmap` + `compliance` added; Epic #98 + issues #99–#119 opened; `/launch-prep` command added.
- **2026-05-25** — #99 code half shipped (`7919edb`): all `localhost:3003` frontend references routed through a single env-driven `BACKEND_URL` helper (`src/config/backend.ts`); slack/twilio/CRM-OAuth de-hardcoded, gmail/contacts/email migrated onto the helper. #99 marked `blocked` on the human-owned deploy (host + `VITE_BACKEND_URL` + provider redirect-URI registration + round-trip verification). Added `/backend-setup` slash command to guide the human through that deploy.
- **2026-05-26** — #100 done (`f1b9e49`): in-app SMS surface gated OFF for v1 behind the new `inAppSms` feature flag (nav entry hidden, `AppView.SMS` route redirects to Dashboard, mock-mode badge unreachable). Real Twilio wiring + flag flip tracked in new follow-up #120 (depends on #99 backend + #109 10DLC).
- **2026-05-26** — #101 sender built (`713476f`): new `send-push` edge function (VAPID Web Push from `push_subscriptions`, secret-guarded, auto-deactivates gone subs) + best-effort wiring into `check-search-alerts` + fixed client public-key fallback (`settings`→`pwa_settings`). Marked `blocked` on the human-owned deploy (VAPID keypair + secrets + Vercel env + `pwa_settings` update + `supabase functions deploy` + live browser test; runbook in `send-push/README.md`).
- **2026-05-26** — #101 **DONE** (verified live `sent:1`). Deploying it surfaced that the push stack was scaffolded in disconnected pieces; reconciled them all: disabled gateway `verify_jwt` for the server-to-server function (`2520c05`); made every notification-enable path actually `subscribe()` to Web Push (store grant/init `06cb825`/`b0303ab`, onboarding `usePermissions` `7d012c6`) — previously they only flipped the browser permission, so `push_subscriptions` stayed empty; injected the push handler into the **VitePWA-generated** SW via `workbox.importScripts` (`a77bc40`) since it supersedes the hand-written `public/sw.js`; added an always-visible Enable/Disable push card to Settings (`619ef86`/`61a2dc9`); pinned `vite preview` to 5173 to reuse the Google OAuth redirect. Human generated the VAPID keypair + `PUSH_DISPATCH_SECRET` and deployed `send-push` on pulse-chat.
- **2026-05-26** — #102 email deliverability code **DONE** (`ec73a65`), issue `status: blocked` on human DNS+deploy+test. Issue evidence was partly stale (`send-email` already on the verified domain), but three other senders still targeted unverified addresses Resend drops/owner-restricts: `send-security-alert` (`onboarding@resend.dev`), `check-search-alerts` (unowned `pulse.ai` defaults), `inviteService` (parent `invites@logosvision.org`). Aligned all to `pulse.logosvision.org` + `reply_to: support@logosvision.org`; fixed `send-email`'s stale docstring; flagged the client-side `VITE_RESEND_API_KEY` exposure with `TODO(#104)`. Added `docs/EMAIL_DELIVERABILITY_RUNBOOK.md` (DMARC TXT record for IONOS, 6-sender inventory, external-inbox test checklist + deploy/curl commands, qntmecos.com-vs-logosvision.org hosting decision = keep logosvision.org for v1). Remaining acceptance (human): add DMARC record, deploy 3 edge fns to `pulse-chat`, run external-inbox delivery test.
- **2026-05-26** — #102 **client key fixed**: `VITE_RESEND_API_KEY` repointed from the dead `jehovahsneaky83` account to the `fm1@qntmecos.com` send-only key in **Vercel** (which had no Resend key set at all) + local `.env` (gitignored, not committed). With the delivery test already passing, **DMARC is the only remaining acceptance item**.
- **2026-05-26** — #102 **delivery test PASSED**: live send from `noreply@pulse.logosvision.org` → `fm1@qntmecos.com` via the `fm1@qntmecos.com` Resend account returned `HTTP 200 {id: f314ac89-…}`. Confirms `pulse.logosvision.org` is verified on the correct account and the verified domain delivers externally → all server-side senders good. Residual = client-side stale-key swap (below) + DMARC.
- **2026-05-26** — #102 live test surfaced a **stale-key bug**. Sending to an external address 403'd ("`pulse.logosvision.org` not verified"); retests showed the parent domain unverified too and the sandbox owner = `jehovahsneaky83@gmail.com`. User confirmed: that's an **abandoned** Resend account; the real account is **`fm1@qntmecos.com`** (domains verified) and the **edge functions use a different key** = that correct account. So server-side email works; only the **client-side** `inviteService`/`rsvpService` are broken because `.env` `VITE_RESEND_API_KEY` still points at the dead account. Fix: repoint the client key to an `fm1` send-only key (+ proper: route through `send-email` edge fn in #104) and add DMARC on the `fm1` domain. Issue updated with full diagnosis.
- **2026-05-26** — #102 edge functions **DEPLOYED** to `pulse-chat` via CLI (send-email v15, send-security-alert v34, check-search-alerts v26). Caught + fixed a deploy footgun (`5b190cc`): `send-security-alert`/`check-search-alerts` were live `verify_jwt=false` but undeclared in `config.toml`, so a plain deploy would have flipped them to the `verify_jwt=true` default and 401'd their `x-cron-secret` calls — added explicit declarations first, verified all three stayed `verify_jwt=false`. #102 remaining (human): add the DMARC TXT record at IONOS + run the external-inbox delivery test.
- **2026-05-26** — #105 email campaigns **DONE + closed** (`0767141`): operator chose to **hide** Campaigns for v1 (feature likely cut). New `emailCampaigns` flag (OFF) mirroring `inAppSms`; the email sidebar "Tools" section (Campaigns its only item) is hidden, the `currentView === 'campaigns'` render is gated on the flag, and a reset effect bounces stale `'campaigns'` state to `'inbox'` → the unsafe per-recipient `emailCampaignService.send()` loop is unreachable in v1. Compliant batched send (rate-limit + `List-Unsubscribe` + suppression + bounce) is the deferred unhide condition. Same run, resolving the #104 audit's 3 open questions per operator decisions: **wired MESSAGE_ANALYTICS into the Intelligence nav section** (`3d51235`, real deployed schema — `in_app_messages` + 2 RPCs), left **Map** in Experimental ("don't abandon, circle back"), and recorded all three in the audit doc (`f0e5c74`).
- **2026-05-26** — #104 feature-flag audit: **audit DONE** (`72c7571`, `docs/FEATURE_FLAG_AUDIT_v1.md`), issue **kept open** as the truth-in-product umbrella. Evidence-based inventory of all ~18 `AppView` surfaces + key sub-surfaces (status / `file:line` / flag / owning issue). Conservative-gating finding: the **only** hard mock in `src/` is `smsService.isMockMode()=>true` (`smsService.ts:40`), already gated via `inAppSms` (#100) — so **no orphan stub needed gating** (valid "gate nothing" outcome). All other non-real surfaces owned by dedicated issues (#105/#106/#107/#108) and cross-referenced, not reworked. Push REAL (#101). Surfaced 3 product calls (Campaigns flag → #105; Map "Experimental" placement; `MESSAGE_ANALYTICS` orphan route). Acceptance #3 click-through = human QA → #115. Closes when #105/#106/#107 land. Also: **closed #102** (operator set the DMARC TXT record — the last acceptance item; email deliverability launch-ready).
- **2026-05-26** — #103 Android release signing **DONE + closed** (`797a132`). Issue evidence was stale (config wasn't `undefined`): a valid keystore (`pulse-release-key.jks`, alias `pulse-key`, SHA384withRSA, valid to 2053) + a `keystore.properties`-reading `signingConfigs.release` already existed, gitignored and never committed. Fixed the real gaps: (1) `npm run android:build` only synced — repurposed it to `android:sync` + new `android:bundle` (`gradlew bundleRelease`) producing the signed AAB, repointed `android:run` to `android:sync` (removes the build/sync duplication); (2) hardened `build.gradle` — creds resolve keystore.properties → `PULSE_KEYSTORE_*` env (CI) → historical defaults, guarded by a `canSignRelease` flag that warns + leaves the artifact UNSIGNED instead of failing cryptically when no keystore is present; (3) wrote `docs/ANDROID_RELEASE_RUNBOOK.md` (fingerprints, properties/env formats, build commands, versionCode rule, upload-key-vs-Play-App-Signing recovery model); (4) allowlisted `docs/*_RUNBOOK.md` in `.gitignore` so runbooks stop falling through the `docs/*.md` ignore. **Verified:** `gradlew bundleRelease` → `BUILD SUCCESSFUL`, signed 20.78 MB AAB at `android/app/build/outputs/bundle/release/app-release.aab` (`PULSE-KE.RSA`/`.SF` from the `pulse-key` alias). Human advisory only (in runbook): offline-backup the keystore + enroll Play App Signing at first upload + bump `versionCode` (252) per release. P0 **code** tier now exhausted.
- **2026-05-27** — #107 'contact enrichment' relabel **DONE + closed** (`c0d0d12`). Premise didn't hold for user-facing surfaces: the React contacts UI was already honest (ContactsRedesigned/DuplicateDetectionModal = "duplicates"/"merge"; `enrichFromSignature` has no UI; no marketing copy). Verified `contactEnrichmentService.ts` = signature-parse + internal `relationship_profiles` dedup/merge + auto-tag, no Clearbit/Apollo/external source. Relabeled the internal mislabel **comment/docstring-only**: service header → `CONTACT PROFILE SERVICE` + "NOT third-party" note; `server.js` `/enrich` endpoint comment clarified as internal relationship intelligence. **Left intact** (regression risk / zero user benefit): cross-app `/enrich` route + `enrichment` response key (Logos Vision contract), ai-router `contact_enrichment` task type, service/file/function names. Comment-only → no new TS errors. **This completes the P1 truth-in-product sweep (#105/#106/#107)** — #104 umbrella now needs only the #115 human QA click-through. *Parallel-session note:* a concurrent Glimpse design session committed the user's playground WIP (`bf55af5`→`f4c0f4c`) mid-run; pause-and-verify held, no loss, and #107's commit used explicit paths so nothing foreign was swept in.
- **2026-05-26** — #106 post-meeting AI **DONE + closed** (`06f3554`). Investigation overturned the issue's premise: **native meeting AI is already REAL** — Deepgram live transcription + ai-router `generateSummary` (action-items/decisions/keyPoints) on Pulse-hosted meetings → `pulse_video_rooms` (acceptance #1, untouched). The genuinely dishonest surface was **Meeting Analytics**: `AnalyticsModal` + the `MeetingsRailKicker` dashboard stats read the `meetings` table for counts/sentiment/topics/decisions, but **nothing in the repo writes that table** (every `.from('meetings')` in `src/` + `supabase/functions/` is a SELECT; native meetings land in `pulse_video_rooms`; Entomate's `meeting.processed` handler posts a *bot recap message*, not analytics rows) — so the surface was permanently empty and its empty-state copy *"Start a Pulse meeting with AI Scribe to populate analytics"* was actively false. Per operator decision (**gate on Entomate**): gated `AnalyticsModal` (new branch order loading → not-connected honest-locked → connected-but-empty honest → data) + the rail-kicker stats on `isEntomateConnected()`, removed the false copy. No coral, reused `--mtg-*`/`meetings-analytics-empty`; no new TS errors vs baseline. Filed **#121** (`launch-roadmap`, `priority: medium`) for the deeper write-back gap — analytics still won't populate even Entomate-connected until a writer exists (repoint `fetchMeetingAnalytics` at `pulse_video_rooms`, or have `ecosystem-inbound` write structured rows). #104 umbrella now needs only #107 + a #115 QA pass to close.
- **2026-05-26** — #99 backend **DEPLOYED** via `/backend-setup` walk-through. `server.js` made `$PORT`-aware (`90b922d`), added `render.yaml` Blueprint (`c293bfe`, native Node, `npm ci --omit=dev`, health check `/api/health`), fixed CRM frontend client-id bug (`f6d9119`, `process.env`→`import.meta.env`). Hosted on Render at `https://pulse-api-1epw.onrender.com` (free plan; recreated as a Blueprint after the root frontend `Dockerfile` was auto-detected and built the wrong artifact). `VITE_BACKEND_URL` set in Vercel + redeploy verified (URL inlined in `svc-email`/`feature-messages` bundles). Verified: `/api/health` 200, Slack/Twilio/Gmail proxies mounted + input-validating, CORS allows `pulse.logosvision.org`. **Deferred (kept #99 open):** live-account round-trip smoke (user-driven); CRM OAuth server-side refactor (`oauthHelper`/`crmService` import the browser-only `supabase` client — blocks #108); LV contacts-import `localhost:5176` redirect.
- **2026-05-27** — #112 ToS / Privacy / moderation + abuse review **DONE + closed** (`a4d5006` co-commit). Operator chose **State of South Carolina** governing law + **`fm1@qntmecos.com`** as canonical legal/privacy/abuse contact everywhere. Biggest substantive fix was truth-in-product: Privacy Policy §5 claimed AI used "anonymized or pseudonymized data" across 5 providers (Gemini, OpenAI, Anthropic, AssemblyAI, ElevenLabs) — false on both counts. Reconciled the AI subprocessor set to the actually-wired three (Gemini + **Anthropic Claude** as primary text per `ai-router/providers.ts`+`tasks.ts` + OpenAI), dropped AssemblyAI/ElevenLabs (zero server-side evidence). Truthful rewrite: user content goes server-side for inference, no client keys, transient and not used for training (intended posture). Also: ToS §13 named **South Carolina** + venue, §6 broadened (AI/Daily.co/Resend/Stripe + /privacy link), §4 added "Reporting Abuse & Takedowns" subsection; Privacy §2.4 honest SMS-v1-disabled note; §10 points to real Settings→Privacy tools (post-#111). DPA card email aligned to `fm1@qntmecos.com`. DSAR runbook intake + AI list updated (added Anthropic — #111's runbook had also understated). **NEW `docs/ABUSE_TAKEDOWN_RUNBOOK.md`** (matches `*_RUNBOOK.md` allowlist): zero-tolerance CSAM with NCMEC CyberTipline, 17 U.S.C. §512(c)(3) DMCA notice + counter-notice + repeat-infringer, real `admin-manage-user` ban/unban/delete (admin-gated, logs to `admin_activity_logs`), user-level email-sender block via `blockedSendersService`, email-based reporting (honest: no in-app Report button in v1). **⚠️ Parallel-session co-commit incident:** a concurrent Claude session on `main` ran `git commit -a` (or `-am`) mid-run, sweeping my explicit-path-staged #112 files (`TermsOfService.tsx`, `PrivacyPolicy.tsx`, `ComplianceSettings.tsx`, `DSAR_RUNBOOK.md`, `ABUSE_TAKEDOWN_RUNBOOK.md`) into its own commit `a4d5006` which is headlined `fix(glimpse): selection mode — gutter checkbox + whole-card toggle`. The diff content of #112 IS correct (verified post-fact in working tree + via `git show --stat a4d5006`); the label is not. CLAUDE.md forbids `push --force` to `main`, so the polluted commit stands with full disclosure on issue #112 and here. Lesson: **explicit-path `git add` doesn't protect against a parallel `git commit -a`** — only branch/worktree isolation does.
- **2026-05-27** — #117 Retention instrumentation + define NSM **DONE + closed** (`a3d3cf7`). Investigation overturned the issue's framing: PostHog + Sentry were in deps and `src/lib/monitoring/` already had ~14 typed event helpers + the `OnboardingEvent` enum + four real emit sites, but the entire stack was **dormant in production** — `initializeMonitoring()` had zero callers anywhere in `src/`, `identifyUser()` likewise never invoked, and the documented "moment of truth" `OnboardingEvent.FirstMessageSent` was defined but never emitted (the `triggerMessage('first_message_sent')` call in `Messages.tsx:2451` belongs to a *different* system, the in-app rule engine, not PostHog). So zero events left the browser, no per-user identity, no D1/D7/D30 cohorts possible. **Shipped end-to-end:** (1) bootstrap `initializeMonitoring()` from `main.tsx:23-32` inside the `!E2E_MAP_HARNESS` guard with try/catch; (2) `identifyUser(session.user.id, ...)` on `SIGNED_IN` + `resetAnalytics()` on `SIGNED_OUT` wired into the existing `onAuthStateChange` callback in `AuthContext.tsx:106-122` — uses only fields already on `User` (`email`, `googleConnected`, `microsoftConnected`), no extra round-trip; (3) trunk `trackMessageSent()` call placed **once** in `pulseService.sendMessage` (`pulseService.ts:386`) after RPC success — single chokepoint for all Pulse DM modes, per-mode spray deliberately avoided per issue note; (4) `trackOnboarding(OnboardingEvent.FirstMessageSent, { workspace_id })` added at `Messages.tsx:2460` **alongside** the existing in-app `triggerMessage` (both must fire — different systems); (5) `OnboardingEvent.TeammateInvited` fires on both `inviteService.ts:144` (Resend) and `:197` (Gmail) paths with a `channel` discriminator so the funnel works whichever route the user takes; (6) stale autocapture URL allowlist `[/pulsemessages\.com/]` (defunct domain) → `[/pulse\.logosvision\.org/, /localhost/]`. **NSM defined** in `docs/PULSE_NSM_AND_RETENTION_GUIDE.md` (~277 lines, allowlisted by `*_GUIDE.md` pattern after the agent's original `PULSE_NSM_AND_RETENTION.md` filename was renamed by the orchestrator to match the gitignore allowlist): **Weekly Active Collaborative Workspaces** = `COUNT(DISTINCT workspace_id) WHERE messages_sent_in_week_w ≥ 50 AND distinct_active_members_in_week_w ≥ 2`. Anchored in the Slack ~2,000-message → 93% weekly-retention finding from audit point 3 — solo workspaces explicitly excluded by design because stickiness is a *multiplayer* property. Doc also defines DAU/MAU stickiness (target 50-70%), D1/D7/D30 cohort definitions, the full activation funnel (`signup → onboarding.surface_shown → onboarding.first_message_sent → onboarding.teammate_invited`), step-by-step PostHog Insight rebuild instructions for the four canonical dashboards, an aha-event reference table (event name × file:line), and a "Known gaps" section calling out: NSM aggregation needs either a Supabase weekly cron or PostHog Group Analytics (paid tier) to compute the workspace-grouped HAVING clause; `FirstCaptureMade` skipped (3+ ambiguous "capture" surfaces in Pulse); trunk `trackMessageSent` doesn't cover Channel/Broadcast/Glimpse since they don't route through `pulseService.sendMessage`. **Verified:** `NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit` shows no new errors on any of the six touched src/ files; the single `analytics.ts:181 posthog.people.increment` type mismatch is pre-existing in `incrementUserProperty` (not in this diff). **Clean commit, no parallel-session interference this run** — explicit-path `git add` for all 7 files, gitleaks pre-commit clean. **Human follow-ups:** set `VITE_POSTHOG_API_KEY` + `VITE_POSTHOG_HOST` in Vercel production once ready to receive events (local dev stays dormant per the existing env gate); build the four PostHog insights from the GUIDE §3 the first time events arrive.
- **2026-05-29** — **Outside-in product review** (three-voice: Skeptic/Enthusiast/Pragmatist) captured in `docs/PULSE_REVIEW_AUDIT_2026-05-29.md` with a machine-readable Findings Ledger (R-01…R-12). `/review-sync` reconciled it: **2 net-new issues filed** — **#124** (public copy README+FAQ advertises non-real features: E2EE, React 19, dropped AI vendors, SMS — public-copy sibling to #104, blocked on #113 for crypto wording) + **#125** (reconcile Decisions "voting" — `proposalMode` OFF/internal vs marketed-as-shipped). Commented review evidence onto #119 (positioning R-07) + #113 (E2EE R-01). Capability Matrix "Decisions + voting" row downgraded ✅→✅/🟡 pending #125. Artifacts produced: `PULSE_FIX_BEFORE_LAUNCH_PUNCHLIST_GUIDE.md` (Skeptic's punch list), `PULSE_MARKETING_COPY_GUIDE.md` (honest landing copy, assumes #119 lane A), `PULSE_README_HONEST_DRAFT_HANDOFF_2026-05-29.md` (drop-in honest README closing #124). New `/review-sync` slash command added to compile future review passes. All other review findings already tracked (#99/#100/#105/#106/#107/#108/#114/#116/#119) or honest-by-hiding (no action).
- **2026-05-27** — #111 GDPR/CCPA data-rights + residency **DONE + closed** (`0ac2922`). Operator chose: keep **us-east-1 (US) + SCCs** for v1 (no EU migration); **full erasure fix with deploy**. Did a DB ground-truth pass first: the `delete_user_account` RPC deleted a broad data set but **never removed `auth.users`** (so the in-app GDPR Art. 17 "right to erasure" claim was untrue — email/identity survived) and skipped several tables with **no FK to `auth.users`** (which therefore never cascade). An FK delete-action audit surfaced **13 NO-ACTION (`a`) columns** (`subtasks.created_by/completed_by`, `task_activity`, `crm_actions`, `crm_contacts`, `crm_deals`, `crm_integrations`, `ecosystem_alerts`, `org_invites`, `org_members`, `share_invites`, `archives.created_by`, `user_sessions.revoked_by_user_id`) that would have **blocked** `auth.admin.deleteUser` with a 23503. Shipped + **deployed to `pulse-chat`**: (1) migration `dsar_erasure_completeness` (`apply_migration`) expanding the RPC — adds the no-cascade tables incl. the **canonical `pulse_users`** row + clears all 13 blockers (own content `DELETE`, actor-on-others'-rows `SET NULL`); (2) new self-serve **`delete-account` edge fn** (deploy v1, `verify_jwt=true`, HTTP-401-verified for unauth) = run RPC through the user's JWT client (so the `auth.uid()` guard passes) → `auth.admin.deleteUser` (CASCADE/SET-NULL FKs clean the rest), audit-logging to `admin_activity_logs` **before** the auth delete to dodge its own FK; (3) wired `DataErasureCard` + `PrivacyDashboard` "Delete Account" to the edge fn; (4) **removed fabricated mock "Download History"** in `PrivacyDashboard` (hardcoded "2.4 GB"/"850 MB" rows) → real `dataExportService.getExportHistory` + honest empty state (truth-in-product); (5) `docs/DSAR_RUNBOOK.md` — residency (us-east-1, SCCs), ROPA, **server-side-AI-touches-user-content note** (ai-router/`gemini-*`/`openai-*`), subprocessor list, DSAR intake (in-app + `legal@quantumecosystems.com`) + SLA (GDPR 1mo / CCPA 45d), precise erasure/retention coverage; (6) `VITE_SUPABASE_REGION` documented + appended to `.env.example` (gitignored → local only). Export path (`DataExportRequestCard`, ~21-table live RLS read, Art. 20) was already solid. Orchestrator fixed one worker bug pre-commit (audit-log ordering vs the auth-delete FK). **Human follow-ups:** set `VITE_SUPABASE_REGION="US East (us-east-1)"` in Vercel; full live deletion test needs a throwaway account; export-completeness for contacts/calendar/email categories is a documented follow-up (runbook §8).
